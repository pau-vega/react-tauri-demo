---
phase: 03-mobile-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - apps/tauri-todo/src/lib/runtime.ts
  - apps/tauri-todo/src/lib/haptics.ts
  - apps/tauri-todo/src/hooks/use-todos.ts
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "apps/tauri-todo/src/lib/runtime.ts exports isTauriRuntime() returning boolean via __TAURI_INTERNALS__ window probe"
    - "apps/tauri-todo/src/lib/haptics.ts exports hapticAdd (impactFeedback medium), hapticToggle (selectionFeedback), hapticDelete (notificationFeedback warning)"
    - "Each haptic wrapper short-circuits off-Tauri via isTauriRuntime() and swallows rejections in try/catch"
    - "useTodos save() propagates failure so CRUD wrappers can gate haptic calls on successful save (refactor from Phase 2 swallow-and-set-error to boolean return)"
    - "addTodo calls void hapticAdd() only after save succeeds; haptic does NOT fire when save fails"
    - "toggleTodo and deleteTodo follow the same success-gated pattern"
    - "Haptic calls are fire-and-forget (void hapticX()) — they do not block the CRUD return"
  artifacts:
    - path: "apps/tauri-todo/src/lib/runtime.ts"
      provides: "isTauriRuntime predicate"
      contains: "__TAURI_INTERNALS__"
    - path: "apps/tauri-todo/src/lib/haptics.ts"
      provides: "Three named haptic wrappers (runtime-guarded, error-swallowing)"
      contains: "impactFeedback"
    - path: "apps/tauri-todo/src/hooks/use-todos.ts"
      provides: "CRUD that fires haptics on successful save only"
      contains: "hapticAdd"
  key_links:
    - from: "apps/tauri-todo/src/lib/haptics.ts"
      to: "@tauri-apps/plugin-haptics"
      via: "imports impactFeedback / notificationFeedback / selectionFeedback"
      pattern: "from \"@tauri-apps/plugin-haptics\""
    - from: "apps/tauri-todo/src/hooks/use-todos.ts"
      to: "@/lib/haptics"
      via: "imports hapticAdd / hapticToggle / hapticDelete; calls void hapticX() after await save()"
      pattern: "void haptic(Add|Toggle|Delete)\\(\\)"
    - from: "save() success path"
      to: "haptic call site"
      via: "save() returns boolean true on success, false on failure — CRUD wrappers short-circuit before haptic when save returned false"
      pattern: "if \\(!(?:ok|saved)\\) return"
---

## Objective

Introduce the two new library modules (runtime.ts and haptics.ts) Plan 01's tests contract against, then refactor use-todos.ts so that (a) save() surfaces success/failure instead of swallowing it, (b) each CRUD wrapper fires its matching void hapticX() only after save() succeeded. The save() signature change is the enabler for the "haptic NOT called when save fails" assertion — Pitfall 6 plus the UX-04 test row in RESEARCH Validation Architecture.

Purpose: UX-04 requires haptic feedback on add/delete. D-10 requires fire-on-success semantics. Pitfall 6 forbids firing on intent. This plan delivers all three.

Output:
- New src/lib/runtime.ts (one exported predicate, three lines of body).
- New src/lib/haptics.ts (three exported wrappers, all runtime-guarded and error-swallowing).
- Refactored src/hooks/use-todos.ts (save() returns boolean; CRUD wrappers fire void hapticX() only on true).

## Execution Context

- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md

## Context

- @.planning/STATE.md
- @.planning/REQUIREMENTS.md
- @.planning/phases/03-mobile-polish/03-CONTEXT.md
- @.planning/phases/03-mobile-polish/03-RESEARCH.md
- @.planning/phases/03-mobile-polish/03-PATTERNS.md
- @apps/tauri-todo/src/hooks/use-todos.ts
- @apps/tauri-todo/src/hooks/use-todos.test.ts

### Interfaces

This plan produces and consumes these exact signatures:

- `apps/tauri-todo/src/lib/runtime.ts`:
  - `export function isTauriRuntime(): boolean`
- `apps/tauri-todo/src/lib/haptics.ts`:
  - `export async function hapticAdd(): Promise<void>`
  - `export async function hapticToggle(): Promise<void>`
  - `export async function hapticDelete(): Promise<void>`
  - Internally imports `impactFeedback`, `notificationFeedback`, `selectionFeedback` from `@tauri-apps/plugin-haptics` (installed by Plan 02).
  - Internally imports `isTauriRuntime` from `@/lib/runtime`.
- `apps/tauri-todo/src/hooks/use-todos.ts`:
  - `save(next: Todo[]): Promise<boolean>` — returns `true` on successful persist, `false` on failure. Error state setting still happens inside save on failure (preserves Phase 2 UX — user sees "Could not load todos" error state).
  - `addTodo` / `toggleTodo` / `deleteTodo` each check `save()`'s return and only call `void hapticX()` when it returned `true`.

Upstream interfaces this plan depends on (from Plans 01 + 02):
- Plan 01's tests: runtime.test.ts, haptics.test.ts, updated use-todos.test.ts — all transition from RED to GREEN after this plan.
- Plan 02: `@tauri-apps/plugin-haptics` installed and its JS bindings importable.

## Tasks

### Task 1: Create src/lib/runtime.ts with isTauriRuntime predicate

- type: auto
- tdd: true
- files: apps/tauri-todo/src/lib/runtime.ts

read_first:
- apps/tauri-todo/src/lib/runtime.test.ts (Plan 01 — defines the exact contract)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Pattern 3 runtime.ts code, State of the Art __TAURI_INTERNALS__ vs legacy)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (runtime.ts section — verbatim snippet)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-14 isTauriRuntime-style guard, RESEARCH Open Question 3 — place in src/lib/runtime.ts)

behavior:
- isTauriRuntime returns true iff typeof window !== "undefined" AND "__TAURI_INTERNALS__" in window.
- No side effects; pure predicate.
- Plan 01 runtime.test.ts transitions from RED (import resolution fails) to GREEN.

action:

Create apps/tauri-todo/src/lib/runtime.ts with this exact content:

```
export function isTauriRuntime(): boolean {
  return typeof window !== "undefined" && "__TAURI_INTERNALS__" in window
}
```

Critical:
- Named export, explicit return type `: boolean` (required for top-level functions per .agents/rules/typescript.md).
- No trailing semicolons anywhere.
- No `any` — uses `in` operator on `window` which TypeScript accepts natively.
- Do NOT add a default export.
- Do NOT JSDoc this — the predicate is self-explanatory.
- File must end with trailing newline.

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/lib/runtime.test.ts

acceptance_criteria:
- `test -f apps/tauri-todo/src/lib/runtime.ts` exits 0.
- `grep -c 'export function isTauriRuntime(): boolean' apps/tauri-todo/src/lib/runtime.ts` returns 1.
- `grep -c '__TAURI_INTERNALS__' apps/tauri-todo/src/lib/runtime.ts` returns 1.
- `grep -c 'typeof window' apps/tauri-todo/src/lib/runtime.ts` returns 1.
- `grep -cE 'export default' apps/tauri-todo/src/lib/runtime.ts` returns 0.
- `grep -nE ';$' apps/tauri-todo/src/lib/runtime.ts` returns 0 (no semicolons).
- `grep -nw 'any' apps/tauri-todo/src/lib/runtime.ts` returns 0.
- Plan 01 contract test passes: `pnpm --filter @monorepo-template/tauri-todo test -- --run src/lib/runtime.test.ts` exits 0.

done: runtime.ts exists with named export isTauriRuntime returning boolean via __TAURI_INTERNALS__ probe. Plan 01 runtime.test.ts goes green.

---

### Task 2: Create src/lib/haptics.ts with three runtime-guarded, error-swallowing wrappers

- type: auto
- tdd: true
- files: apps/tauri-todo/src/lib/haptics.ts

read_first:
- apps/tauri-todo/src/lib/haptics.test.ts (Plan 01 — defines exact contract for each wrapper: plugin called with correct literal, errors swallowed, off-runtime no-op)
- apps/tauri-todo/src/lib/runtime.ts (Task 1 output — isTauriRuntime to import)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Pattern 3 haptics.ts code + Pitfall 2 error swallow + State of the Art plugin API)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (haptics.ts section — error-swallow pattern adapted from use-todos.save() with D-13 divergence: no state mutation in catch)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-10/D-11/D-12 intensity mapping, D-13 fire-and-forget, D-14 runtime guard)

behavior:
- Three named async Promise<void> wrappers (`hapticAdd`, `hapticToggle`, `hapticDelete`).
- Each: early-return if !isTauriRuntime(); then try/await the mapped plugin call; catch swallows silently (NO state mutation, NO rethrow).
- Intensity mapping:
  - hapticAdd → impactFeedback("medium")
  - hapticToggle → selectionFeedback()
  - hapticDelete → notificationFeedback("warning")
- Plan 01 haptics.test.ts transitions from RED to GREEN across all three describe blocks.

action:

Create apps/tauri-todo/src/lib/haptics.ts with this exact content:

```
import { impactFeedback, notificationFeedback, selectionFeedback } from "@tauri-apps/plugin-haptics"

import { isTauriRuntime } from "@/lib/runtime"

export async function hapticAdd(): Promise<void> {
  if (!isTauriRuntime()) return
  try {
    await impactFeedback("medium")
  } catch {
    // Android vibration support is device-dependent (plugin docs).
    // Never break a CRUD op over a haptic failure.
  }
}

export async function hapticToggle(): Promise<void> {
  if (!isTauriRuntime()) return
  try {
    await selectionFeedback()
  } catch {
    // swallow — haptic must never break a CRUD op
  }
}

export async function hapticDelete(): Promise<void> {
  if (!isTauriRuntime()) return
  try {
    await notificationFeedback("warning")
  } catch {
    // swallow — haptic must never break a CRUD op
  }
}
```

Critical:
- Top-level `import type` NOT needed here — all three plugin imports are runtime values.
- Imports alphabetized within groups per perfectionist/sort-imports: plugin imports first (third-party scope), blank line, then internal `@/` imports.
- Three named exports; no default export.
- Explicit return type `: Promise<void>` on each top-level function.
- Zero rethrow inside catch blocks — silent swallow only. NO setState, NO logger call.
- Comment inside each catch block (brief) — avoids an empty block triggering ESLint `no-empty`.
- Literal strings `"medium"` and `"warning"` — not `as const` (plugin API already types these as literal unions per RESEARCH §State of the Art).
- No `any`; no generic types needed.
- No trailing semicolons.

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/lib/haptics.test.ts

acceptance_criteria:
- `test -f apps/tauri-todo/src/lib/haptics.ts` exits 0.
- `grep -c 'export async function hapticAdd(): Promise<void>' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'export async function hapticToggle(): Promise<void>' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'export async function hapticDelete(): Promise<void>' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'impactFeedback("medium")' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'selectionFeedback()' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'notificationFeedback("warning")' apps/tauri-todo/src/lib/haptics.ts` returns 1.
- `grep -c 'isTauriRuntime' apps/tauri-todo/src/lib/haptics.ts` returns 4 (one import + three guard calls).
- `grep -c 'try {' apps/tauri-todo/src/lib/haptics.ts` returns 3.
- `grep -c '} catch {' apps/tauri-todo/src/lib/haptics.ts` returns 3.
- `grep -cE 'export default' apps/tauri-todo/src/lib/haptics.ts` returns 0.
- `grep -nw 'any' apps/tauri-todo/src/lib/haptics.ts` returns 0.
- `grep -nE ';$' apps/tauri-todo/src/lib/haptics.ts` returns 0.
- Plan 01 contract test passes: `pnpm --filter @monorepo-template/tauri-todo test -- --run src/lib/haptics.test.ts` exits 0.

done: haptics.ts exists with three runtime-guarded, error-swallowing wrappers. Plan 01 haptics.test.ts goes green across all four describe blocks (off-Tauri no-op, in-runtime correct call, correct intensity, errors swallowed).

---

### Task 3: Refactor useTodos so save() surfaces success, CRUD wrappers gate haptic on success

- type: auto
- tdd: true
- files: apps/tauri-todo/src/hooks/use-todos.ts

read_first:
- apps/tauri-todo/src/hooks/use-todos.ts (current file — save() lines 45-57 swallow errors and set state internally)
- apps/tauri-todo/src/hooks/use-todos.test.ts (Plan 01 extended — mocks `@/lib/haptics` and asserts call-order + no-call-on-save-failure)
- apps/tauri-todo/src/lib/haptics.ts (Task 2 output — the three wrappers to call)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-10/D-11/D-12 on successful store.save resolve, D-13 fire-and-forget, D-14 runtime guard)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Pattern 3 use-todos.ts CRUD snippet + Pitfall 6 fire-after-save)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (use-todos.ts section — "save() refactor gap" call-out, Correctness constraints)

behavior:
- save() refactored: returns `Promise<boolean>` — `true` when store.set + store.save both resolve AND state transitions to ready; `false` when the catch block fires. State transition on error (to `{ status: "error", message }`) preserved.
- addTodo: after `const ok = await save(next)`, call `if (!ok) return` before `void hapticAdd()`. No-op guards (empty text, trim==="") stay unchanged and NEVER reach save() (so haptic correctly does not fire per Plan 01's "is a no-op" test assertions).
- toggleTodo and deleteTodo mirror the pattern.
- `void hapticX()` (fire-and-forget) — NOT `await hapticX()`. Haptic latency (50-150ms) must not serialize into the CRUD resolve.
- No API shape change for the returned hook object (`{ state, addTodo, toggleTodo, deleteTodo }` preserved — the CRUD wrappers still return `Promise<void>`).
- All Plan 01 use-todos.test.ts assertions transition from RED to GREEN, including the three save-failure tests.

action:

Overwrite apps/tauri-todo/src/hooks/use-todos.ts with this exact content:

```
import type { Store } from "@tauri-apps/plugin-store"

import { load } from "@tauri-apps/plugin-store"
import { useEffect, useRef, useState } from "react"

import { hapticAdd, hapticDelete, hapticToggle } from "@/lib/haptics"

export type Todo = {
  id: string
  text: string
  completed: boolean
}

export type TodosState =
  | { status: "loading" }
  | { status: "ready"; todos: Todo[] }
  | { status: "error"; message: string }

export function useTodos() {
  const [state, setState] = useState<TodosState>({ status: "loading" })
  const storeRef = useRef<Store | null>(null)
  const todosRef = useRef<Todo[]>([])

  useEffect(() => {
    let cancelled = false
    async function init() {
      try {
        const store = await load("store.json", { autoSave: false, defaults: {} })
        if (cancelled) return
        storeRef.current = store
        const stored = await store.get<Todo[]>("todos")
        if (cancelled) return
        todosRef.current = stored ?? []
        setState({ status: "ready", todos: todosRef.current })
      } catch (err) {
        if (cancelled) return
        const message = err instanceof Error ? err.message : String(err)
        setState({ status: "error", message })
      }
    }
    void init()
    return () => {
      cancelled = true
    }
  }, [])

  async function save(next: Todo[]): Promise<boolean> {
    const store = storeRef.current
    if (!store) return false
    try {
      await store.set("todos", next)
      await store.save()
      todosRef.current = next
      setState({ status: "ready", todos: next })
      return true
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      setState({ status: "error", message })
      return false
    }
  }

  async function addTodo(text: string) {
    if (!storeRef.current) return
    const trimmed = text.trim()
    if (trimmed.length === 0) return
    const next: Todo[] = [...todosRef.current, { id: crypto.randomUUID(), text: trimmed, completed: false }]
    const ok = await save(next)
    if (!ok) return
    void hapticAdd()
  }

  async function toggleTodo(id: string) {
    if (!storeRef.current) return
    const next = todosRef.current.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))
    const ok = await save(next)
    if (!ok) return
    void hapticToggle()
  }

  async function deleteTodo(id: string) {
    if (!storeRef.current) return
    const next = todosRef.current.filter((t) => t.id !== id)
    const ok = await save(next)
    if (!ok) return
    void hapticDelete()
  }

  return { state, addTodo, toggleTodo, deleteTodo }
}
```

Critical:
- save() signature changed: was `async function save(next: Todo[])` returning implicit `Promise<void>`; now `async function save(next: Todo[]): Promise<boolean>`. Every code path returns a boolean (the `if (!store) return false` early-return, the `return true` after successful persist, the `return false` in the catch).
- State transition on failure preserved — this keeps Phase 2 UX (TodoList showing "Could not load todos. Restart the app and try again." per UI-SPEC Copywriting) unchanged. The only semantic addition is the boolean return.
- `if (!ok) return` placed immediately after the `const ok = await save(next)` line — Pitfall 6 enforcement. Haptic MUST be unreachable on save-failure.
- `void hapticX()` — fire-and-forget; must NOT be awaited. RESEARCH Pitfall 4 ("don't await haptic calls").
- No new state added — `ok` is a local const per call.
- Imports alphabetized per perfectionist: type imports first (`import type { Store } ...`), blank line, runtime imports alphabetized (`load`, `useEffect` etc), blank line, internal `@/` imports. The new import `import { hapticAdd, hapticDelete, hapticToggle } from "@/lib/haptics"` lives in the internal-imports group.
- Preserve `crypto.randomUUID()`, `todosRef.current.map(...)`, `todosRef.current.filter(...)`, Phase 2 init effect, and TodosState discriminated union verbatim.
- No `any`, no `enum`, no default export, no semicolons, 120-char width.

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/hooks/use-todos.test.ts

acceptance_criteria:
- `grep -c 'async function save(next: Todo\[\]): Promise<boolean>' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- `grep -c 'return true' apps/tauri-todo/src/hooks/use-todos.ts` returns 1 (success path in save).
- `grep -c 'return false' apps/tauri-todo/src/hooks/use-todos.ts` returns 2 (no-store early-return + catch path).
- `grep -cE 'const ok = await save\(next\)' apps/tauri-todo/src/hooks/use-todos.ts` returns 3 (addTodo, toggleTodo, deleteTodo).
- `grep -cE 'if \(!ok\) return' apps/tauri-todo/src/hooks/use-todos.ts` returns 3 (one per CRUD wrapper, gating the haptic).
- `grep -c 'void hapticAdd()' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- `grep -c 'void hapticToggle()' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- `grep -c 'void hapticDelete()' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- No awaited haptic calls: `grep -cE 'await haptic(Add|Toggle|Delete)' apps/tauri-todo/src/hooks/use-todos.ts` returns 0.
- State-on-error preserved: `grep -c 'status: "error"' apps/tauri-todo/src/hooks/use-todos.ts` returns at least 2 (init catch + save catch).
- Imports group correct: `grep -c 'import { hapticAdd, hapticDelete, hapticToggle } from "@/lib/haptics"' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- API shape preserved: `grep -c 'return { state, addTodo, toggleTodo, deleteTodo }' apps/tauri-todo/src/hooks/use-todos.ts` returns 1.
- No `any`: `grep -nw any apps/tauri-todo/src/hooks/use-todos.ts` returns 0.
- No default export: `grep -cE 'export default' apps/tauri-todo/src/hooks/use-todos.ts` returns 0.
- Plan 01 extended test passes: `pnpm --filter @monorepo-template/tauri-todo test -- --run src/hooks/use-todos.test.ts` exits 0 and includes the new "does not call hapticX when save() fails" assertions.

done: save() returns Promise<boolean>; CRUD wrappers gate haptic on success; fire-and-forget via void; all Plan 01 use-todos assertions (including save-failure cases) go green. Phase 2 error-state UX preserved.

---

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → Tauri Rust IPC | Each haptic wrapper invokes plugin bindings that marshal via Tauri's JSON-IPC channel. Gated by capabilities/mobile.json (Plan 02). |
| WebView JS → DOM window globals | isTauriRuntime probes `window.__TAURI_INTERNALS__`, the current authoritative Tauri-presence signal (State of the Art note in RESEARCH.md). |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | D (Denial of Service) | Unhandled promise rejection in haptic call crashes CRUD save flow | mitigate | Every plugin call is inside `try { await ... } catch { /* swallow */ }` in haptics.ts (D-13). Haptic failure never rethrows. Plan 01 haptics.test.ts "errors swallowed" describe block asserts this. |
| T-03-12 | T (Tampering) | Haptic fires on a save-failure scenario, misleading the user into believing their todo was saved | mitigate | save() returns Promise<boolean>; CRUD wrappers `if (!ok) return` before `void hapticX()`. Plan 01's three "does not call hapticX when save() fails" tests assert this ordering. |
| T-03-13 | I (Information Disclosure) | User todo text leaks through haptic IPC | accept | haptic wrappers pass only literal `"medium"` / `"warning"` strings — no user data. Todo text never reaches the haptics plugin surface. |
| T-03-14 | E (Elevation of Privilege) | isTauriRuntime false-positive on web host causes haptic call on unsupported surface | mitigate | isTauriRuntime predicate uses the authoritative `window.__TAURI_INTERNALS__` probe (State of the Art per RESEARCH). Even if a false positive slipped through, the error-swallow wrapper catches the resulting rejection — defense in depth. |

Reference: Plan 02 creates the Tauri-IPC trust boundary via capability grants. This plan consumes that boundary from the JS side.

## Verification

Full-suite run after all three tasks:

    pnpm --filter @monorepo-template/tauri-todo test -- --run

Expected result: every test file from Plans 01-04 green — including runtime.test.ts, haptics.test.ts, capabilities.test.ts (via Plan 02), todo-app.test.tsx, updated todo-input.test.tsx, updated todo-item.test.tsx, updated use-todos.test.ts.

Followed by:

    pnpm --filter @monorepo-template/tauri-todo typecheck
    pnpm --filter @monorepo-template/tauri-todo lint

Both must exit 0.

Manual/visual verification (D-19 on-device) happens in Plan 05.

## Success Criteria

- runtime.ts exports isTauriRuntime returning boolean via __TAURI_INTERNALS__ probe.
- haptics.ts exports the three named wrappers with correct intensity mapping and error-swallow try/catch.
- use-todos.ts save() returns Promise<boolean>; CRUD wrappers gate haptic on success; fire-and-forget via void.
- State-on-error UX preserved; hook API shape preserved.
- All Plan 01 tests pass (runtime, haptics, use-todos).
- typecheck + lint pass.

## Output

After completion, create .planning/phases/03-mobile-polish/03-04-SUMMARY.md summarizing:
- Exact save() signature change (before/after).
- Where the haptic-gating `if (!ok) return` lands per CRUD wrapper.
- Rationale for `void hapticX()` over `await hapticX()` (Pitfall 4).
- Which Plan 01 tests flipped from red to green (runtime, haptics, use-todos extended + save-failure cases).
