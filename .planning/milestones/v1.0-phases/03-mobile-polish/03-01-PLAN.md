---
phase: 03-mobile-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tauri-todo/src/lib/runtime.test.ts
  - apps/tauri-todo/src/lib/haptics.test.ts
  - apps/tauri-todo/src/lint/no-hover.test.ts
  - apps/tauri-todo/src/lint/no-ui-package.test.ts
  - apps/tauri-todo/src/lint/capabilities.test.ts
  - apps/tauri-todo/src/components/todo-app.test.tsx
  - apps/tauri-todo/src/components/todo-input.test.tsx
  - apps/tauri-todo/src/components/todo-item.test.tsx
  - apps/tauri-todo/src/hooks/use-todos.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
must_haves:
  truths:
    - "A failing test exists for every VALIDATION.md Wave 0 row before implementation begins"
    - "isTauriRuntime behavior is contract-tested (false when window lacks __TAURI_INTERNALS__, true when present)"
    - "hapticAdd, hapticToggle, hapticDelete contracts are captured (runtime-guard, correct plugin call, intensity args, error swallow)"
    - "apps/tauri-todo/src is guarded against future reintroduction of hover: utilities via an FS-grep unit test"
    - "apps/tauri-todo is guarded against future reintroduction of @monorepo-template/ui via an FS-grep unit test"
    - "capabilities/mobile.json, lib.rs, Cargo.toml correctness is contract-tested via string-match assertions"
    - "TodoInput h-11, TodoItem w-11 h-11, and TodoApp safe-area padding assertions are captured"
    - "addTodo, toggleTodo, deleteTodo call haptic wrappers after save() resolves and NOT on save failure"
  artifacts:
    - path: "apps/tauri-todo/src/lib/runtime.test.ts"
      provides: "Contract tests for isTauriRuntime"
      contains: "isTauriRuntime"
    - path: "apps/tauri-todo/src/lib/haptics.test.ts"
      provides: "Contract tests for haptic wrappers"
      contains: "impactFeedback"
    - path: "apps/tauri-todo/src/lint/no-hover.test.ts"
      provides: "FS-grep lint - zero hover: utilities under src/"
      contains: "hover:"
    - path: "apps/tauri-todo/src/lint/no-ui-package.test.ts"
      provides: "FS-grep lint - zero @monorepo-template/ui imports/deps"
      contains: "@monorepo-template/ui"
    - path: "apps/tauri-todo/src/lint/capabilities.test.ts"
      provides: "Config correctness - mobile.json / lib.rs / Cargo.toml"
      contains: "haptics:allow-impact-feedback"
    - path: "apps/tauri-todo/src/components/todo-app.test.tsx"
      provides: "TodoApp main safe-area padding class assertions"
      contains: "env(safe-area-inset-top)"
  key_links:
    - from: "apps/tauri-todo/src/hooks/use-todos.test.ts"
      to: "@/lib/haptics"
      via: "vi.mock + mocked module assertions"
      pattern: "vi.mock.*@/lib/haptics"
    - from: "apps/tauri-todo/src/lib/haptics.test.ts"
      to: "@tauri-apps/plugin-haptics"
      via: "vi.mock with three vi.fn exports"
      pattern: "vi.mock.*@tauri-apps/plugin-haptics"
---

## Objective

Create every MISSING test file from VALIDATION.md Wave 0 and extend the three existing tests (use-todos.test.ts, todo-input.test.tsx, todo-item.test.tsx) with new Phase 3 behavior contracts. Every test defines behavior downstream plans must implement. Tests MUST fail now and MUST pass after Plans 03 and 04 land.

Purpose: Nyquist-compliant sampling requires every UX-01..04 success criterion have a failing automated test before its implementation begins. This plan produces the contract surface; Plans 03 and 04 satisfy it.

Output: 6 new test files (runtime guard, haptic wrappers, hover lint, UI-package lint, capability/Rust/Cargo install integrity, TodoApp safe-area). 3 modified test files (44px class-string assertions + haptic call-order + save-failure semantics).

## Execution Context

- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md

## Context

- @.planning/STATE.md
- @.planning/REQUIREMENTS.md
- @.planning/phases/03-mobile-polish/03-CONTEXT.md
- @.planning/phases/03-mobile-polish/03-RESEARCH.md
- @.planning/phases/03-mobile-polish/03-PATTERNS.md
- @.planning/phases/03-mobile-polish/03-VALIDATION.md
- @.planning/phases/03-mobile-polish/03-UI-SPEC.md
- @apps/tauri-todo/src/hooks/use-todos.test.ts
- @apps/tauri-todo/src/components/todo-input.test.tsx
- @apps/tauri-todo/src/components/todo-item.test.tsx

### Interfaces

Contracts the Wave-0 tests assert against. Downstream plans MUST match these signatures.

- apps/tauri-todo/src/lib/runtime.ts (created in Plan 04): exports function isTauriRuntime returning boolean.
- apps/tauri-todo/src/lib/haptics.ts (Plan 04) exports three async Promise<void> functions:
  - hapticAdd maps to impactFeedback with argument "medium"
  - hapticToggle maps to selectionFeedback called with no args
  - hapticDelete maps to notificationFeedback with argument "warning"
- @tauri-apps/plugin-haptics (installed in Plan 02) exports impactFeedback(style), notificationFeedback(type), selectionFeedback(). Style union: "light" | "medium" | "heavy" | "soft" | "rigid". Type union: "success" | "warning" | "error".
- use-todos.ts (post-Plan-04 refactor): each CRUD calls matching hapticX AFTER save() resolves and MUST NOT call the haptic when save() fails.
- UI-SPEC class contracts:
  - TodoInput input + Add button: h-11
  - TodoItem toggle (both branches): w-11 h-11
  - TodoItem delete button: w-11 h-11
  - TodoApp main: contains pt-[max(2rem,env(safe-area-inset-top))] and pb-[max(2rem,env(safe-area-inset-bottom))]
- Post-Plan-02 config contracts:
  - capabilities/mobile.json permissions array contains all three literal haptic permission strings
  - src-tauri/src/lib.rs contains .setup(|app|, #[cfg(mobile)], tauri_plugin_haptics::init()
  - src-tauri/Cargo.toml contains [target.'cfg(any(target_os = "android", target_os = "ios"))'.dependencies] and tauri-plugin-haptics = "2.3.2"

## Tasks

### Task 1: Create runtime + haptics unit test files

- type: auto
- tdd: true
- files: apps/tauri-todo/src/lib/runtime.test.ts, apps/tauri-todo/src/lib/haptics.test.ts

read_first:
- apps/tauri-todo/src/hooks/use-todos.test.ts (canonical vi.mock analog — copy imports, describe/it/beforeEach/vi.clearAllMocks, as-unknown-as-Store boundary cast)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (section for haptics.test.ts — verbatim vi.mock shape and window-guard stub pattern)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Validation Architecture UX-04 rows)
- .planning/phases/03-mobile-polish/03-VALIDATION.md (Wave 0 list)

behavior:
- runtime.test.ts: isTauriRuntime returns false when window.__TAURI_INTERNALS__ is absent (default jsdom), returns true after assigning an empty object, and false again after delete.
- haptics.test.ts — off-Tauri: hapticAdd/Toggle/Delete resolve without calling their plugin mocks.
- haptics.test.ts — in runtime: hapticAdd calls impactFeedback("medium") exactly once; hapticToggle calls selectionFeedback() exactly once with no args; hapticDelete calls notificationFeedback("warning") exactly once; each wrapper does NOT call the other two plugin functions.
- haptics.test.ts — errors swallowed: when the plugin mock rejects via mockRejectedValueOnce, the wrapper resolves without rethrowing (D-13).

action:

Create apps/tauri-todo/src/lib/runtime.test.ts with this exact content (Prettier semi:false, no semicolons, 120-char width, imports alphabetized per perfectionist):

```
import { afterEach, beforeEach, describe, expect, it } from "vitest"

import { isTauriRuntime } from "@/lib/runtime"

type WindowWithTauri = { __TAURI_INTERNALS__?: unknown }

describe("isTauriRuntime", () => {
  beforeEach(() => {
    delete (window as unknown as WindowWithTauri).__TAURI_INTERNALS__
  })
  afterEach(() => {
    delete (window as unknown as WindowWithTauri).__TAURI_INTERNALS__
  })

  it("returns false when window.__TAURI_INTERNALS__ is absent", () => {
    expect(isTauriRuntime()).toBe(false)
  })

  it("returns true when window.__TAURI_INTERNALS__ is present", () => {
    ;(window as unknown as WindowWithTauri).__TAURI_INTERNALS__ = {}
    expect(isTauriRuntime()).toBe(true)
  })

  it("returns false again after __TAURI_INTERNALS__ is removed", () => {
    ;(window as unknown as WindowWithTauri).__TAURI_INTERNALS__ = {}
    expect(isTauriRuntime()).toBe(true)
    delete (window as unknown as WindowWithTauri).__TAURI_INTERNALS__
    expect(isTauriRuntime()).toBe(false)
  })
})
```

Create apps/tauri-todo/src/lib/haptics.test.ts with this exact content:

```
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest"

import { impactFeedback, notificationFeedback, selectionFeedback } from "@tauri-apps/plugin-haptics"

import { hapticAdd, hapticDelete, hapticToggle } from "@/lib/haptics"

vi.mock("@tauri-apps/plugin-haptics", () => ({
  impactFeedback: vi.fn(async () => undefined),
  notificationFeedback: vi.fn(async () => undefined),
  selectionFeedback: vi.fn(async () => undefined),
}))

type WindowWithTauri = { __TAURI_INTERNALS__?: unknown }

beforeEach(() => {
  vi.clearAllMocks()
  delete (window as unknown as WindowWithTauri).__TAURI_INTERNALS__
})
afterEach(() => {
  delete (window as unknown as WindowWithTauri).__TAURI_INTERNALS__
})

describe("haptics wrappers - off-Tauri no-op", () => {
  it("hapticAdd does not call impactFeedback when __TAURI_INTERNALS__ is absent", async () => {
    await hapticAdd()
    expect(impactFeedback).not.toHaveBeenCalled()
  })
  it("hapticToggle does not call selectionFeedback when __TAURI_INTERNALS__ is absent", async () => {
    await hapticToggle()
    expect(selectionFeedback).not.toHaveBeenCalled()
  })
  it("hapticDelete does not call notificationFeedback when __TAURI_INTERNALS__ is absent", async () => {
    await hapticDelete()
    expect(notificationFeedback).not.toHaveBeenCalled()
  })
})

describe("haptics wrappers - in Tauri runtime", () => {
  beforeEach(() => {
    ;(window as unknown as WindowWithTauri).__TAURI_INTERNALS__ = {}
  })

  it("hapticAdd calls impactFeedback medium exactly once and does not call the others", async () => {
    await hapticAdd()
    expect(impactFeedback).toHaveBeenCalledExactlyOnceWith("medium")
    expect(selectionFeedback).not.toHaveBeenCalled()
    expect(notificationFeedback).not.toHaveBeenCalled()
  })

  it("hapticToggle calls selectionFeedback exactly once with no args", async () => {
    await hapticToggle()
    expect(selectionFeedback).toHaveBeenCalledExactlyOnceWith()
    expect(impactFeedback).not.toHaveBeenCalled()
    expect(notificationFeedback).not.toHaveBeenCalled()
  })

  it("hapticDelete calls notificationFeedback warning exactly once", async () => {
    await hapticDelete()
    expect(notificationFeedback).toHaveBeenCalledExactlyOnceWith("warning")
    expect(impactFeedback).not.toHaveBeenCalled()
    expect(selectionFeedback).not.toHaveBeenCalled()
  })
})

describe("haptics wrappers - errors swallowed", () => {
  beforeEach(() => {
    ;(window as unknown as WindowWithTauri).__TAURI_INTERNALS__ = {}
  })

  it("hapticAdd resolves without rethrowing when impactFeedback rejects", async () => {
    vi.mocked(impactFeedback).mockRejectedValueOnce(new Error("boom"))
    await expect(hapticAdd()).resolves.toBeUndefined()
  })
  it("hapticToggle resolves without rethrowing when selectionFeedback rejects", async () => {
    vi.mocked(selectionFeedback).mockRejectedValueOnce(new Error("boom"))
    await expect(hapticToggle()).resolves.toBeUndefined()
  })
  it("hapticDelete resolves without rethrowing when notificationFeedback rejects", async () => {
    vi.mocked(notificationFeedback).mockRejectedValueOnce(new Error("boom"))
    await expect(hapticDelete()).resolves.toBeUndefined()
  })
})
```

NOTE: these tests WILL FAIL now because @/lib/runtime, @/lib/haptics, and the @tauri-apps/plugin-haptics package do not exist yet. Failure is expected (Nyquist). Plan 02 creates the package; Plan 04 creates the library modules.

verify (automated):

test -f apps/tauri-todo/src/lib/runtime.test.ts && test -f apps/tauri-todo/src/lib/haptics.test.ts && grep -q "isTauriRuntime" apps/tauri-todo/src/lib/runtime.test.ts && grep -q "impactFeedback" apps/tauri-todo/src/lib/haptics.test.ts

acceptance_criteria:
- apps/tauri-todo/src/lib/runtime.test.ts exists.
- apps/tauri-todo/src/lib/haptics.test.ts exists.
- grep -c "isTauriRuntime" apps/tauri-todo/src/lib/runtime.test.ts returns >= 4.
- grep -c "impactFeedback" apps/tauri-todo/src/lib/haptics.test.ts returns >= 3.
- grep -c "selectionFeedback" apps/tauri-todo/src/lib/haptics.test.ts returns >= 3.
- grep -c "notificationFeedback" apps/tauri-todo/src/lib/haptics.test.ts returns >= 3.
- grep -F 'vi.mock("@tauri-apps/plugin-haptics"' apps/tauri-todo/src/lib/haptics.test.ts returns >= 1.
- grep -F '__TAURI_INTERNALS__' apps/tauri-todo/src/lib/haptics.test.ts returns >= 2.
- No `any` keyword: grep -nw "any" apps/tauri-todo/src/lib/runtime.test.ts apps/tauri-todo/src/lib/haptics.test.ts returns 0 matches.
- No trailing semicolons: grep -nE ";$" apps/tauri-todo/src/lib/runtime.test.ts apps/tauri-todo/src/lib/haptics.test.ts returns 0.

done: Both files exist with required describe/it structure, the required mock of @tauri-apps/plugin-haptics, and the window-guard stubbing pattern. Tests currently fail at import resolution (expected).

---

### Task 2: Create the three filesystem lint test files

- type: auto
- tdd: true
- files: apps/tauri-todo/src/lint/no-hover.test.ts, apps/tauri-todo/src/lint/no-ui-package.test.ts, apps/tauri-todo/src/lint/capabilities.test.ts

read_first:
- apps/tauri-todo/src/hooks/use-todos.test.ts (Vitest skeleton - describe/it/expect)
- apps/tauri-todo/src-tauri/capabilities/mobile.json (current permissions array)
- apps/tauri-todo/src-tauri/src/lib.rs (current 7-line file)
- apps/tauri-todo/src-tauri/Cargo.toml (current [dependencies])
- apps/tauri-todo/package.json (current dependencies)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (lint test skeleton section)
- .planning/phases/03-mobile-polish/03-VALIDATION.md (Wave 0 rows)

behavior:
- no-hover.test.ts walks apps/tauri-todo/src recursively via fs.promises.readdir with recursive:true and asserts NO .ts/.tsx/.css file (excluding .test.ts/.test.tsx) contains "hover:".
- no-ui-package.test.ts walks apps/tauri-todo/src and asserts no source file contains "@monorepo-template/ui"; additionally reads package.json and asserts neither dependencies nor devDependencies contain the key "@monorepo-template/ui".
- capabilities.test.ts asserts mobile.json permissions array contains all three haptic permission strings; lib.rs matches /\.setup\(\s*\|app\|/, /#\[cfg\(mobile\)\]/, /tauri_plugin_haptics::init\(\)/; Cargo.toml contains the literal target-cfg string and `tauri-plugin-haptics = "2.3.2"`.

action:

Create apps/tauri-todo/src/lint/no-hover.test.ts with this exact content:

```
import { readFile, readdir } from "node:fs/promises"
import { resolve } from "node:path"
import { describe, expect, it } from "vitest"

const SRC_DIR = resolve(import.meta.dirname, "..")
const EXTS = [".ts", ".tsx", ".css"]

async function collectFiles(dir: string): Promise<string[]> {
  const entries = await readdir(dir, { recursive: true, withFileTypes: true })
  const out: string[] = []
  for (const entry of entries) {
    if (!entry.isFile()) continue
    if (!EXTS.some((ext) => entry.name.endsWith(ext))) continue
    if (entry.name.endsWith(".test.ts") || entry.name.endsWith(".test.tsx")) continue
    const parent = entry.parentPath ?? dir
    out.push(resolve(parent, entry.name))
  }
  return out
}

describe("no hover: utilities in apps/tauri-todo/src", () => {
  it("does not contain any Tailwind hover: utility in source files", async () => {
    const files = await collectFiles(SRC_DIR)
    const offenders: string[] = []
    for (const file of files) {
      const content = await readFile(file, "utf8")
      if (/\bhover:/.test(content)) offenders.push(file)
    }
    expect(offenders).toEqual([])
  })
})
```

Create apps/tauri-todo/src/lint/no-ui-package.test.ts with this exact content:

```
import { readFile, readdir } from "node:fs/promises"
import { resolve } from "node:path"
import { describe, expect, it } from "vitest"

const SRC_DIR = resolve(import.meta.dirname, "..")
const PKG_JSON = resolve(import.meta.dirname, "../../package.json")
const EXTS = [".ts", ".tsx", ".css"]
const FORBIDDEN = "@monorepo-template/ui"

async function collectFiles(dir: string): Promise<string[]> {
  const entries = await readdir(dir, { recursive: true, withFileTypes: true })
  const out: string[] = []
  for (const entry of entries) {
    if (!entry.isFile()) continue
    if (!EXTS.some((ext) => entry.name.endsWith(ext))) continue
    if (entry.name.endsWith(".test.ts") || entry.name.endsWith(".test.tsx")) continue
    const parent = entry.parentPath ?? dir
    out.push(resolve(parent, entry.name))
  }
  return out
}

describe("no @monorepo-template/ui imports or deps in apps/tauri-todo", () => {
  it("does not import @monorepo-template/ui anywhere in src/", async () => {
    const files = await collectFiles(SRC_DIR)
    const offenders: string[] = []
    for (const file of files) {
      const content = await readFile(file, "utf8")
      if (content.includes(FORBIDDEN)) offenders.push(file)
    }
    expect(offenders).toEqual([])
  })

  it("does not declare @monorepo-template/ui in dependencies or devDependencies", async () => {
    const raw = await readFile(PKG_JSON, "utf8")
    const pkg = JSON.parse(raw) as { dependencies?: Record<string, string>; devDependencies?: Record<string, string> }
    const allKeys = [...Object.keys(pkg.dependencies ?? {}), ...Object.keys(pkg.devDependencies ?? {})]
    expect(allKeys).not.toContain(FORBIDDEN)
  })
})
```

Create apps/tauri-todo/src/lint/capabilities.test.ts with this exact content:

```
import { readFile } from "node:fs/promises"
import { resolve } from "node:path"
import { describe, expect, it } from "vitest"

const SRC_TAURI = resolve(import.meta.dirname, "../../src-tauri")

describe("haptics install integrity", () => {
  it("capabilities/mobile.json grants all three haptics:allow-* permissions", async () => {
    const raw = await readFile(resolve(SRC_TAURI, "capabilities/mobile.json"), "utf8")
    const json = JSON.parse(raw) as { permissions: string[] }
    expect(json.permissions).toContain("haptics:allow-impact-feedback")
    expect(json.permissions).toContain("haptics:allow-notification-feedback")
    expect(json.permissions).toContain("haptics:allow-selection-feedback")
  })

  it("lib.rs registers tauri_plugin_haptics inside a mobile-gated .setup() block", async () => {
    const rs = await readFile(resolve(SRC_TAURI, "src/lib.rs"), "utf8")
    expect(rs).toMatch(/\.setup\(\s*\|app\|/)
    expect(rs).toMatch(/#\[cfg\(mobile\)\]/)
    expect(rs).toMatch(/tauri_plugin_haptics::init\(\)/)
  })

  it("Cargo.toml declares tauri-plugin-haptics 2.3.2 under a mobile target cfg section", async () => {
    const toml = await readFile(resolve(SRC_TAURI, "Cargo.toml"), "utf8")
    expect(toml).toContain(`[target.'cfg(any(target_os = "android", target_os = "ios"))'.dependencies]`)
    expect(toml).toMatch(/tauri-plugin-haptics\s*=\s*"2\.3\.2"/)
  })
})
```

NOTE: no-hover and no-ui-package pass now (repo already satisfies D-17/D-18). capabilities.test.ts fails until Plan 02 completes.

verify (automated):

test -f apps/tauri-todo/src/lint/no-hover.test.ts && test -f apps/tauri-todo/src/lint/no-ui-package.test.ts && test -f apps/tauri-todo/src/lint/capabilities.test.ts && grep -q "haptics:allow-impact-feedback" apps/tauri-todo/src/lint/capabilities.test.ts

acceptance_criteria:
- All three files exist under apps/tauri-todo/src/lint/.
- grep -c "readdir" apps/tauri-todo/src/lint/no-hover.test.ts returns >= 1.
- grep -c "hover:" apps/tauri-todo/src/lint/no-hover.test.ts returns >= 1.
- grep -c "@monorepo-template/ui" apps/tauri-todo/src/lint/no-ui-package.test.ts returns >= 2 (constant + JSON-keys assertion).
- grep -c "haptics:allow-impact-feedback" apps/tauri-todo/src/lint/capabilities.test.ts returns >= 1.
- grep -c "haptics:allow-notification-feedback" apps/tauri-todo/src/lint/capabilities.test.ts returns >= 1.
- grep -c "haptics:allow-selection-feedback" apps/tauri-todo/src/lint/capabilities.test.ts returns >= 1.
- grep -c "tauri_plugin_haptics::init" apps/tauri-todo/src/lint/capabilities.test.ts returns >= 1.
- grep -F 'target_os = "android"' apps/tauri-todo/src/lint/capabilities.test.ts returns >= 1.
- No `any` keyword: grep -nw "any" apps/tauri-todo/src/lint/*.test.ts returns 0 matches.

done: All three files exist with the exact regex patterns and JSON assertions above. no-hover and no-ui-package pass now; capabilities.test.ts fails until Plan 02 lands.

---

### Task 3: Create TodoApp test + extend TodoInput and TodoItem tests with 44px assertions

- type: auto
- tdd: true
- files: apps/tauri-todo/src/components/todo-app.test.tsx, apps/tauri-todo/src/components/todo-input.test.tsx, apps/tauri-todo/src/components/todo-item.test.tsx

read_first:
- apps/tauri-todo/src/components/todo-input.test.tsx (existing — extend, do not replace)
- apps/tauri-todo/src/components/todo-item.test.tsx (existing — extend, do not replace)
- apps/tauri-todo/src/components/todo-app.tsx (current main class: min-h-screen bg-white px-4 py-8)
- apps/tauri-todo/src/components/todo-input.tsx (current h-10 strings)
- apps/tauri-todo/src/components/todo-item.tsx (current w-6 h-6 toggle + w-8 h-8 delete)
- apps/tauri-todo/src/hooks/use-todos.test.ts (vi.mock pattern for plugin-store — TodoApp test mocks the same module so the hook initializes cleanly under jsdom)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (todo-app.test.tsx section)
- .planning/phases/03-mobile-polish/03-UI-SPEC.md (Component Inventory)

behavior:
- todo-app.test.tsx: render TodoApp with plugin-store mocked, await main appearance, assert className contains pt-[max(2rem,env(safe-area-inset-top))] and pb-[max(2rem,env(safe-area-inset-bottom))], and does NOT contain py-8.
- todo-input.test.tsx: new it that asserts input className matches /\bh-11\b/ and NOT /\bh-10\b/; new it that asserts the Add button className matches /\bh-11\b/ and NOT /\bh-10\b/.
- todo-item.test.tsx: new its asserting toggle className matches /\bw-11\b/ and /\bh-11\b/ when completed=false (and NOT w-6/h-6), same when completed=true, and the delete button className matches /\bw-11\b/ and /\bh-11\b/ (and NOT w-8/h-8).

action:

Create apps/tauri-todo/src/components/todo-app.test.tsx with this exact content:

```
import type { Store } from "@tauri-apps/plugin-store"

import { load } from "@tauri-apps/plugin-store"
import { render, waitFor } from "@testing-library/react"
import { describe, expect, it, vi } from "vitest"

import { TodoApp } from "@/components/todo-app"

vi.mock("@tauri-apps/plugin-store", () => ({
  load: vi.fn(),
}))

function createStoreDouble() {
  const data: Record<string, unknown> = {}
  return {
    get: vi.fn(async (key: string) => data[key]),
    set: vi.fn(async (key: string, value: unknown) => {
      data[key] = value
    }),
    save: vi.fn(async () => undefined),
  }
}

describe("TodoApp safe-area padding", () => {
  it("renders main with max(2rem, env(safe-area-inset-*)) top and bottom padding", async () => {
    vi.mocked(load).mockResolvedValue(createStoreDouble() as unknown as Store)

    const { container } = render(<TodoApp />)
    await waitFor(() => {
      expect(container.querySelector("main")).not.toBeNull()
    })

    const main = container.querySelector("main")
    if (!main) throw new Error("expected main element")

    expect(main.className).toContain("pt-[max(2rem,env(safe-area-inset-top))]")
    expect(main.className).toContain("pb-[max(2rem,env(safe-area-inset-bottom))]")
    expect(main.className).not.toMatch(/\bpy-8\b/)
  })
})
```

Extend apps/tauri-todo/src/components/todo-input.test.tsx — APPEND these two it blocks INSIDE the existing describe("TodoInput", () => { ... }) (right before the closing }) of that describe). Do NOT rewrite any existing test:

```
  it("renders the input with h-11 (44px) and not h-10", () => {
    render(<TodoInput disabled={false} onAdd={vi.fn(async () => undefined)} />)
    const input = screen.getByPlaceholderText("Add a todo...")
    expect(input.className).toMatch(/\bh-11\b/)
    expect(input.className).not.toMatch(/\bh-10\b/)
  })

  it("renders the Add button with h-11 (44px) and not h-10", () => {
    render(<TodoInput disabled={false} onAdd={vi.fn(async () => undefined)} />)
    const button = screen.getByRole("button", { name: /add/i })
    expect(button.className).toMatch(/\bh-11\b/)
    expect(button.className).not.toMatch(/\bh-10\b/)
  })
```

Extend apps/tauri-todo/src/components/todo-item.test.tsx — APPEND these three it blocks INSIDE the existing describe("TodoItem", () => { ... }) (right before the closing }) of that describe). Do NOT rewrite any existing test:

```
  it("renders the toggle at 44px (w-11 h-11) when todo is incomplete", () => {
    render(
      <TodoItem
        onDelete={vi.fn(async () => undefined)}
        onToggle={vi.fn(async () => undefined)}
        todo={makeTodo({ completed: false })}
      />,
    )
    const toggle = screen.getByRole("button", { name: "Mark as complete" })
    expect(toggle.className).toMatch(/\bw-11\b/)
    expect(toggle.className).toMatch(/\bh-11\b/)
    expect(toggle.className).not.toMatch(/\bw-6\b/)
    expect(toggle.className).not.toMatch(/\bh-6\b/)
  })

  it("renders the toggle at 44px (w-11 h-11) when todo is completed", () => {
    render(
      <TodoItem
        onDelete={vi.fn(async () => undefined)}
        onToggle={vi.fn(async () => undefined)}
        todo={makeTodo({ completed: true })}
      />,
    )
    const toggle = screen.getByRole("button", { name: "Mark as incomplete" })
    expect(toggle.className).toMatch(/\bw-11\b/)
    expect(toggle.className).toMatch(/\bh-11\b/)
  })

  it("renders the delete button at 44px (w-11 h-11) and not w-8/h-8", () => {
    render(
      <TodoItem
        onDelete={vi.fn(async () => undefined)}
        onToggle={vi.fn(async () => undefined)}
        todo={makeTodo()}
      />,
    )
    const del = screen.getByRole("button", { name: /delete todo/i })
    expect(del.className).toMatch(/\bw-11\b/)
    expect(del.className).toMatch(/\bh-11\b/)
    expect(del.className).not.toMatch(/\bw-8\b/)
    expect(del.className).not.toMatch(/\bh-8\b/)
  })
```

NOTE: all new assertions fail now (todo-app has py-8; todo-input has h-10; todo-item has w-6 h-6 and w-8 h-8). Plan 03 makes them pass.

verify (automated):

test -f apps/tauri-todo/src/components/todo-app.test.tsx && grep -q "env(safe-area-inset-top)" apps/tauri-todo/src/components/todo-app.test.tsx && grep -q "h-11" apps/tauri-todo/src/components/todo-input.test.tsx && grep -q "w-11" apps/tauri-todo/src/components/todo-item.test.tsx

acceptance_criteria:
- apps/tauri-todo/src/components/todo-app.test.tsx exists.
- grep -F 'pt-[max(2rem,env(safe-area-inset-top))]' apps/tauri-todo/src/components/todo-app.test.tsx returns >= 1.
- grep -F 'pb-[max(2rem,env(safe-area-inset-bottom))]' apps/tauri-todo/src/components/todo-app.test.tsx returns >= 1.
- grep -F 'vi.mock("@tauri-apps/plugin-store"' apps/tauri-todo/src/components/todo-app.test.tsx returns >= 1.
- grep -c "h-11" apps/tauri-todo/src/components/todo-input.test.tsx returns >= 2.
- grep -c "h-10" apps/tauri-todo/src/components/todo-input.test.tsx returns >= 2.
- grep -c "w-11" apps/tauri-todo/src/components/todo-item.test.tsx returns >= 3.
- grep -c "h-11" apps/tauri-todo/src/components/todo-item.test.tsx returns >= 3.
- Existing tests preserved: grep -c 'describe("TodoInput"' apps/tauri-todo/src/components/todo-input.test.tsx returns 1; grep -c 'describe("TodoItem"' apps/tauri-todo/src/components/todo-item.test.tsx returns 1.
- Original it blocks still present: grep -c "submits the trimmed text" apps/tauri-todo/src/components/todo-input.test.tsx >= 1; grep -c "invokes onDelete" apps/tauri-todo/src/components/todo-item.test.tsx >= 1.

done: Three component test files updated as specified. Assertions fail now; Plan 03 makes them pass.

---

### Task 4: Extend use-todos.test.ts with haptic mock + call-order + no-call-on-save-failure assertions

- type: auto
- tdd: true
- files: apps/tauri-todo/src/hooks/use-todos.test.ts

read_first:
- apps/tauri-todo/src/hooks/use-todos.test.ts (existing — extend, do not replace)
- apps/tauri-todo/src/hooks/use-todos.ts (current CRUD shape — save() currently swallows errors; Plan 04 refactors save() to surface failure)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (use-todos.test.ts section)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Validation Architecture UX-04 + Pitfall 6)

behavior:
- Module-level vi.mock("@/lib/haptics", ...) with three vi.fn wrappers.
- Existing addTodo success test asserts hapticAdd called once; others not called.
- Existing toggle test asserts hapticToggle called; others not.
- Existing delete test asserts hapticDelete called; others not.
- Two existing no-op addTodo tests assert hapticAdd NOT called.
- Three NEW it blocks assert the matching haptic NOT called when save() throws (depends on Plan 04 save() refactor).

action:

Extend apps/tauri-todo/src/hooks/use-todos.test.ts. Preserve all existing imports and tests. Apply these edits:

Step 1 — add this import line immediately after the existing import { useTodos } from "@/hooks/use-todos" line (alphabetical within the @/ group per perfectionist):

```
import { hapticAdd, hapticDelete, hapticToggle } from "@/lib/haptics"
```

Step 2 — add the module mock immediately after the existing vi.mock("@tauri-apps/plugin-store", ...) block:

```
vi.mock("@/lib/haptics", () => ({
  hapticAdd: vi.fn(async () => undefined),
  hapticToggle: vi.fn(async () => undefined),
  hapticDelete: vi.fn(async () => undefined),
}))
```

Step 3 — inside the existing test titled "adds a new todo with trimmed text, fresh id, and completed=false, persisting to the store", APPEND at the end (after the existing expect(double.save).toHaveBeenCalled() line):

```
    expect(hapticAdd).toHaveBeenCalledTimes(1)
    expect(hapticToggle).not.toHaveBeenCalled()
    expect(hapticDelete).not.toHaveBeenCalled()
```

Step 4 — inside the test titled "is a no-op when addTodo receives whitespace-only text", APPEND at the end:

```
    expect(hapticAdd).not.toHaveBeenCalled()
```

Step 5 — inside the test titled "is a no-op when addTodo receives an empty string", APPEND at the end:

```
    expect(hapticAdd).not.toHaveBeenCalled()
```

Step 6 — inside the test titled "toggles only the matching todo's completed flag, leaving other todos unchanged", APPEND at the end:

```
    expect(hapticToggle).toHaveBeenCalledTimes(1)
    expect(hapticAdd).not.toHaveBeenCalled()
    expect(hapticDelete).not.toHaveBeenCalled()
```

Step 7 — inside the test titled "deletes only the matching todo, leaving other todos unchanged and in order", APPEND at the end:

```
    expect(hapticDelete).toHaveBeenCalledTimes(1)
    expect(hapticAdd).not.toHaveBeenCalled()
    expect(hapticToggle).not.toHaveBeenCalled()
```

Step 8 — add three NEW it blocks just before the final closing }) of describe("useTodos", () => { ... }):

```
  it("does not call hapticAdd when save() fails after addTodo", async () => {
    const double = createStoreDouble()
    double.save = vi.fn(async () => {
      throw new Error("disk full")
    })
    vi.mocked(load).mockResolvedValue(double as unknown as Store)

    const { result } = renderHook(() => useTodos())
    await waitFor(() => expect(result.current.state.status).toBe("ready"))

    await act(async () => {
      await result.current.addTodo("Buy milk")
    })

    expect(hapticAdd).not.toHaveBeenCalled()
  })

  it("does not call hapticToggle when save() fails after toggleTodo", async () => {
    const existing = [{ id: "id-a", text: "a", completed: false }]
    const double = createStoreDouble({ todos: existing })
    double.save = vi.fn(async () => {
      throw new Error("disk full")
    })
    vi.mocked(load).mockResolvedValue(double as unknown as Store)

    const { result } = renderHook(() => useTodos())
    await waitFor(() => expect(result.current.state.status).toBe("ready"))

    await act(async () => {
      await result.current.toggleTodo("id-a")
    })

    expect(hapticToggle).not.toHaveBeenCalled()
  })

  it("does not call hapticDelete when save() fails after deleteTodo", async () => {
    const existing = [{ id: "id-a", text: "a", completed: false }]
    const double = createStoreDouble({ todos: existing })
    double.save = vi.fn(async () => {
      throw new Error("disk full")
    })
    vi.mocked(load).mockResolvedValue(double as unknown as Store)

    const { result } = renderHook(() => useTodos())
    await waitFor(() => expect(result.current.state.status).toBe("ready"))

    await act(async () => {
      await result.current.deleteTodo("id-a")
    })

    expect(hapticDelete).not.toHaveBeenCalled()
  })
```

NOTE: Steps 3-7 fail now (import resolution — @/lib/haptics does not yet exist). Step 8 also fails due to resolution AND because the current save() swallows errors (haptic would fire even on failure). Plan 04 creates the module AND refactors save() so all these pass.

verify (automated):

grep -F 'vi.mock("@/lib/haptics"' apps/tauri-todo/src/hooks/use-todos.test.ts && grep -q "hapticAdd" apps/tauri-todo/src/hooks/use-todos.test.ts && grep -q "does not call hapticAdd when save" apps/tauri-todo/src/hooks/use-todos.test.ts

acceptance_criteria:
- grep -F 'vi.mock("@/lib/haptics"' apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 1.
- grep -F '@/lib/haptics' apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 2 (import + mock).
- grep -c "hapticAdd" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 7.
- grep -c "hapticToggle" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 5.
- grep -c "hapticDelete" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 5.
- grep -c "disk full" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 3.
- grep -c "does not call hapticAdd when save" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 1.
- grep -c "does not call hapticToggle when save" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 1.
- grep -c "does not call hapticDelete when save" apps/tauri-todo/src/hooks/use-todos.test.ts returns >= 1.
- Original tests preserved: grep -c "loads an empty list from a fresh store" apps/tauri-todo/src/hooks/use-todos.test.ts >= 1; grep -c "transitions to error status when the initial store load fails" apps/tauri-todo/src/hooks/use-todos.test.ts >= 1.

done: use-todos.test.ts extended with haptics mock, call-order assertions in existing tests, and three new save-failure assertions. Plan 04 makes them pass.

---

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (net-new) | This plan creates only test files. No new runtime surface, no IPC, no capability change. |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | Regression — future plan silently drops haptic wrappers or hover-free invariant | mitigate | FS-grep lint tests run on every pnpm test invocation. Any regression fails the full-suite gate. |
| T-03-02 | I (Information Disclosure) | Test assertions embed secrets or PII | accept | Tests assert only on class strings, literal plugin call args ("medium" / "warning"), and filesystem presence. No PII, no secrets. |

Reference: no new IPC, no new capability grant, no new external data flow. See Plan 02 for the capability permission boundary introduced by the haptics plugin install.

## Verification

Run the full Vitest suite and observe the expected failure pattern:

    pnpm --filter @monorepo-template/tauri-todo test -- --run

Expected:
- PASS (already satisfied): no-hover.test.ts, no-ui-package.test.ts, every original test that was not extended.
- FAIL by design (Nyquist): haptics.test.ts, runtime.test.ts, capabilities.test.ts, todo-app.test.tsx, new assertions in todo-input.test.tsx and todo-item.test.tsx, new assertions in use-todos.test.ts.

Plans 02, 03, 04 turn every "fails by design" test green. Lint + typecheck may surface missing modules @/lib/runtime and @/lib/haptics; that is acceptable at this wave.

## Success Criteria

- All 9 file modifications in files_modified are on disk.
- The three FS-lint tests use the exact collectFiles helper shape.
- vi.mock for @tauri-apps/plugin-haptics is present in haptics.test.ts and lists all three bindings.
- vi.mock for @/lib/haptics is present in use-todos.test.ts and lists all three wrappers.
- All new assertions use word-boundary regex or exact string-literal matching as specified.
- No `any`, no default exports, no trailing semicolons on any new line.
- Pre-existing tests remain structurally intact (describe names and existing it titles present).

## Output

After completion, create .planning/phases/03-mobile-polish/03-01-SUMMARY.md summarizing:
- Files created vs files extended
- Which tests pass immediately and which fail by design (Nyquist handoff to Plans 02-04)
- Any deviations from the action text (there should be none)
