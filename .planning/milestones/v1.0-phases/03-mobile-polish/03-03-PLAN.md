---
phase: 03-mobile-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/tauri-todo/src/components/todo-input.tsx
  - apps/tauri-todo/src/components/todo-item.tsx
  - apps/tauri-todo/src/components/todo-app.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
must_haves:
  truths:
    - "TodoInput text input has height class h-11 (44px)"
    - "TodoInput Add button has height class h-11 (44px)"
    - "TodoItem toggle (both completed and incomplete variants) has w-11 h-11 (44x44px)"
    - "TodoItem delete button has w-11 h-11 (44x44px)"
    - "TodoItem row padding remains at px-3 py-3 (D-06 unchanged)"
    - "TodoApp main element has pt-[max(2rem,env(safe-area-inset-top))] and pb-[max(2rem,env(safe-area-inset-bottom))]"
    - "apps/tauri-todo/src contains zero hover: Tailwind utilities (UX-02 re-verified)"
    - "apps/tauri-todo has zero imports of @monorepo-template/ui (UX-03 re-verified)"
  artifacts:
    - path: "apps/tauri-todo/src/components/todo-input.tsx"
      provides: "44px input + Add button"
      contains: "h-11"
    - path: "apps/tauri-todo/src/components/todo-item.tsx"
      provides: "44px toggle + delete controls"
      contains: "w-11 h-11"
    - path: "apps/tauri-todo/src/components/todo-app.tsx"
      provides: "Safe-area padding on main"
      contains: "env(safe-area-inset-top)"
  key_links:
    - from: "apps/tauri-todo/src/components/todo-app.tsx"
      to: "WebView viewport-fit=cover meta tag (Phase 1 D-25)"
      via: "CSS env(safe-area-inset-*) tokens"
      pattern: "env\\(safe-area-inset-"
    - from: "apps/tauri-todo/src/components/todo-input.tsx + todo-item.tsx"
      to: "Phase 2 active:opacity-90 press pattern"
      via: "preserved verbatim on enlarged controls"
      pattern: "active:opacity-90"
---

## Objective

Grow all four interactive touch targets (TodoInput input, TodoInput Add button, TodoItem toggle, TodoItem delete) from their current 24-40px sizes to 44px square / 44px tall per D-01..D-05. Replace `<main>`'s `py-8` with safe-area inset padding per D-15/D-16. Re-verify UX-02 (no hover:) and UX-03 (no @monorepo-template/ui) via Plan 01's FS-lint tests — no code changes needed there, just confirm the invariants still hold after this plan's refactor.

Purpose: UX-01 requires every interactive control meet the 44px floor. UX-02/UX-03 are verified-only invariants. This plan delivers all three, plus the safe-area inset padding from D-15.

Output: Three modified React component files with class-string bumps. No JSX structural changes. No new imports. No new components.

## Execution Context

- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
- @/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md

## Context

- @.planning/STATE.md
- @.planning/REQUIREMENTS.md
- @.planning/phases/03-mobile-polish/03-CONTEXT.md
- @.planning/phases/03-mobile-polish/03-RESEARCH.md
- @.planning/phases/03-mobile-polish/03-PATTERNS.md
- @.planning/phases/03-mobile-polish/03-UI-SPEC.md
- @apps/tauri-todo/src/components/todo-input.tsx
- @apps/tauri-todo/src/components/todo-item.tsx
- @apps/tauri-todo/src/components/todo-app.tsx

### Interfaces

Post-plan contracts Plan 01 tests assert:
- TodoInput: input element className matches `/\bh-11\b/` and NOT `/\bh-10\b/`. Add button className matches `/\bh-11\b/` and NOT `/\bh-10\b/`. All other classes on these elements preserved verbatim.
- TodoItem: toggle className (both branches) matches `/\bw-11\b/` and `/\bh-11\b/` and NOT `/\bw-6\b/` or `/\bh-6\b/`. Delete button className matches `/\bw-11\b/` and `/\bh-11\b/` and NOT `/\bw-8\b/` or `/\bh-8\b/`. Row container className (`<li>`) stays `flex items-center gap-3 px-3 py-3 rounded-lg bg-white border border-gray-200` (D-06). All other classes preserved.
- TodoApp: main className contains `pt-[max(2rem,env(safe-area-inset-top))]` and `pb-[max(2rem,env(safe-area-inset-bottom))]`. Does NOT contain `py-8`. Other classes on main (`min-h-screen bg-white px-4`) preserved. Existing inner div / h1 / TodoInput / TodoList structure preserved.

Post-plan verification (no code change in this plan, just assertion):
- Plan 01's no-hover.test.ts and no-ui-package.test.ts continue to pass (invariants preserved after refactor).

## Tasks

### Task 1: Grow TodoInput input and Add button to h-11 (44px)

- type: auto
- tdd: true
- files: apps/tauri-todo/src/components/todo-input.tsx

read_first:
- apps/tauri-todo/src/components/todo-input.tsx (current file — lines 25 and 31 have h-10)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-02 input h-10 to h-11, D-03 button h-10 to h-11)
- .planning/phases/03-mobile-polish/03-UI-SPEC.md (Touch target floor 44px table)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (todo-input.tsx section — before/after class strings)

behavior:
- Input element height changes from h-10 (40px) to h-11 (44px). All other classes preserved verbatim.
- Add button height changes from h-10 to h-11. All other classes preserved verbatim.
- `disabled:opacity-50`, `active:opacity-90`, `focus:ring-blue-700` all preserved.
- Plan 01's todo-input.test.tsx assertions `/\bh-11\b/` on both input and button go from RED to GREEN.

action:

Overwrite apps/tauri-todo/src/components/todo-input.tsx with this exact content (the ONLY changes from the current file are `h-10` to `h-11` on the two elements):

```
import { useState } from "react"

type TodoInputProps = {
  onAdd: (text: string) => Promise<void>
  disabled: boolean
}

export function TodoInput({ onAdd, disabled }: TodoInputProps) {
  const [text, setText] = useState("")

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    const trimmed = text.trim()
    if (trimmed.length === 0) return
    await onAdd(trimmed)
    setText("")
  }

  const canSubmit = !disabled && text.trim().length > 0

  return (
    <form className="flex gap-2" onSubmit={handleSubmit}>
      <input
        autoFocus
        className="flex-1 h-11 px-3 bg-white border border-gray-200 rounded-md text-base text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-700"
        onChange={(e) => setText(e.target.value)}
        placeholder="Add a todo..."
        value={text}
      />
      <button
        className="shrink-0 h-11 px-4 bg-blue-700 text-white text-sm font-normal rounded-md active:opacity-90 disabled:opacity-50"
        disabled={!canSubmit}
        type="submit"
      >
        Add
      </button>
    </form>
  )
}
```

Critical:
- No hover: classes introduced. Do NOT add `hover:opacity-90` etc.
- No other class tokens changed — compare diff carefully. Only `h-10` becomes `h-11` (two occurrences).
- Do NOT import from @monorepo-template/ui (UX-03 invariant).
- Preserve the existing `autoFocus`, form `flex gap-2`, placeholder copy `"Add a todo..."`, and button label `Add`.

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-input.test.tsx

acceptance_criteria:
- `grep -c '\bh-11\b' apps/tauri-todo/src/components/todo-input.tsx` returns 2 (input + button).
- `grep -c '\bh-10\b' apps/tauri-todo/src/components/todo-input.tsx` returns 0.
- `grep -c '\bhover:' apps/tauri-todo/src/components/todo-input.tsx` returns 0.
- `grep -c 'active:opacity-90' apps/tauri-todo/src/components/todo-input.tsx` returns 1 (press pattern preserved).
- `grep -c 'disabled:opacity-50' apps/tauri-todo/src/components/todo-input.tsx` returns 1.
- `grep -c 'focus:ring-blue-700' apps/tauri-todo/src/components/todo-input.tsx` returns 1.
- `grep -c 'placeholder="Add a todo..."' apps/tauri-todo/src/components/todo-input.tsx` returns 1 (copy preserved).
- `grep -cE '(@monorepo-template/ui|from "@monorepo-template/ui)' apps/tauri-todo/src/components/todo-input.tsx` returns 0.
- Plan 01 todo-input.test.tsx new assertions now pass: `pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-input.test.tsx` exits 0.

done: Input and button both h-11; all other classes, props, copy preserved; Plan 01 assertions go green.

---

### Task 2: Grow TodoItem toggle and delete controls to w-11 h-11 (44x44px)

- type: auto
- tdd: true
- files: apps/tauri-todo/src/components/todo-item.tsx

read_first:
- apps/tauri-todo/src/components/todo-item.tsx (current file — lines 10-12 have w-6 h-6; line 29 has w-8 h-8)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-04 toggle w-6 h-6 to w-11 h-11, D-05 delete w-8 h-8 to w-11 h-11, D-06 row padding unchanged)
- .planning/phases/03-mobile-polish/03-UI-SPEC.md (Touch target table, Typography — toggle checkmark text-white; delete glyph text-xl leading-none; Color section — active:text-red-600 on delete)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (todo-item.tsx section)

behavior:
- Toggle (both completed-branch and incomplete-branch className strings) gets w-11 h-11.
- Delete button gets w-11 h-11.
- Row container (`<li>`) keeps `px-3 py-3` verbatim (D-06).
- Checkmark glyph scaled from `text-xs` to `text-sm` for visual balance inside the larger toggle container (Claude's discretion inside UI-SPEC guidance: "recommended text-sm or text-base"; `text-sm` chosen for measured growth without dominating the control).
- Delete glyph `×` stays `text-xl leading-none` (UI-SPEC Typography).
- `active:opacity-90` preserved on toggle; `active:opacity-90 active:text-red-600` preserved on delete (Phase 2 press pattern).
- aria-labels preserved verbatim: `"Mark as complete"` / `"Mark as incomplete"` / `"Delete todo"`.

action:

Overwrite apps/tauri-todo/src/components/todo-item.tsx with this exact content:

```
import type { Todo } from "@/hooks/use-todos"

type TodoItemProps = {
  todo: Todo
  onToggle: (id: string) => Promise<void>
  onDelete: (id: string) => Promise<void>
}

export function TodoItem({ todo, onToggle, onDelete }: TodoItemProps) {
  const toggleClass = todo.completed
    ? "w-11 h-11 shrink-0 rounded-full bg-blue-700 border-2 border-blue-700 flex items-center justify-center active:opacity-90"
    : "w-11 h-11 shrink-0 rounded-full border-2 border-gray-300 bg-white flex items-center justify-center active:opacity-90"

  const textClass = todo.completed ? "flex-1 text-base line-through text-gray-400" : "flex-1 text-base text-gray-900"

  return (
    <li className="flex items-center gap-3 px-3 py-3 rounded-lg bg-white border border-gray-200">
      <button
        aria-label={todo.completed ? "Mark as incomplete" : "Mark as complete"}
        className={toggleClass}
        onClick={() => onToggle(todo.id)}
        type="button"
      >
        {todo.completed && <span className="text-white text-sm">✓</span>}
      </button>
      <span className={textClass}>{todo.text}</span>
      <button
        aria-label="Delete todo"
        className="w-11 h-11 shrink-0 flex items-center justify-center rounded-md text-gray-400 active:opacity-90 active:text-red-600"
        onClick={() => onDelete(todo.id)}
        type="button"
      >
        <span className="text-xl leading-none">×</span>
      </button>
    </li>
  )
}
```

Critical:
- Exactly three occurrences of `w-11 h-11` (toggle completed + toggle incomplete + delete button). Zero occurrences of `w-6 h-6`, `w-8 h-8`, or any other un-migrated size.
- Row (`<li>`) class `flex items-center gap-3 px-3 py-3 rounded-lg bg-white border border-gray-200` preserved verbatim (D-06 row padding stays).
- Toggle checkmark span goes from `text-white text-xs` to `text-white text-sm` (Claude's discretion per UI-SPEC; chosen for balance with larger container).
- No hover: classes introduced anywhere.
- aria-labels preserved exactly: `"Mark as complete"`, `"Mark as incomplete"`, `"Delete todo"`.
- Do NOT import from @monorepo-template/ui.
- Preserve `import type { Todo } from "@/hooks/use-todos"` (type-only import per TS convention).

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-item.test.tsx

acceptance_criteria:
- `grep -c 'w-11 h-11' apps/tauri-todo/src/components/todo-item.tsx` returns 3 (toggle completed, toggle incomplete, delete).
- `grep -c '\bw-6\b' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- `grep -c '\bh-6\b' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- `grep -c '\bw-8\b' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- `grep -c '\bh-8\b' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- Row padding preserved: `grep -c 'px-3 py-3' apps/tauri-todo/src/components/todo-item.tsx` returns 1.
- Press patterns preserved: `grep -c 'active:opacity-90' apps/tauri-todo/src/components/todo-item.tsx` returns 3 (two toggle branches + one delete); `grep -c 'active:text-red-600' apps/tauri-todo/src/components/todo-item.tsx` returns 1.
- aria-labels preserved: `grep -c 'Mark as complete' apps/tauri-todo/src/components/todo-item.tsx` returns 1; `grep -c 'Mark as incomplete' apps/tauri-todo/src/components/todo-item.tsx` returns 1; `grep -c 'Delete todo' apps/tauri-todo/src/components/todo-item.tsx` returns 1.
- `grep -c 'text-white text-sm' apps/tauri-todo/src/components/todo-item.tsx` returns 1 (checkmark scaled).
- `grep -c 'text-xl leading-none' apps/tauri-todo/src/components/todo-item.tsx` returns 1 (delete glyph preserved).
- No hover: classes: `grep -c '\bhover:' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- No UI-package import: `grep -c '@monorepo-template/ui' apps/tauri-todo/src/components/todo-item.tsx` returns 0.
- Plan 01 todo-item.test.tsx now passes: `pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-item.test.tsx` exits 0.

done: Both toggle branches and the delete button are w-11 h-11; row padding unchanged; press patterns and aria-labels preserved; Plan 01 assertions go green.

---

### Task 3: Apply safe-area inset padding to TodoApp main element + re-verify UX-02/UX-03 invariants

- type: auto
- tdd: true
- files: apps/tauri-todo/src/components/todo-app.tsx

read_first:
- apps/tauri-todo/src/components/todo-app.tsx (current `<main className="min-h-screen bg-white px-4 py-8">`)
- .planning/phases/03-mobile-polish/03-CONTEXT.md (D-15 apply env(safe-area-inset-top/bottom), D-16 Tailwind v4 arbitrary value, D-17 zero hover: still required, D-18 no UI package imports still required)
- .planning/phases/03-mobile-polish/03-RESEARCH.md (Pattern 4 Tailwind v4 safe-area, Pitfall 5 max(2rem, ...) fallback)
- .planning/phases/03-mobile-polish/03-UI-SPEC.md (Spacing — Safe-area inset padding table)
- .planning/phases/03-mobile-polish/03-PATTERNS.md (todo-app.tsx section)

behavior:
- `<main>` className replaces `py-8` with `pt-[max(2rem,env(safe-area-inset-top))]` and `pb-[max(2rem,env(safe-area-inset-bottom))]`.
- All other classes on main preserved: `min-h-screen bg-white px-4`.
- Inner div (max-w-md container) preserved verbatim.
- JSX children preserved: h1, TodoInput, TodoList.
- Plan 01's todo-app.test.tsx assertion goes from RED to GREEN.
- Plan 01's no-hover.test.ts and no-ui-package.test.ts stay GREEN (this plan does not introduce hover: or ui-package imports anywhere).

action:

Overwrite apps/tauri-todo/src/components/todo-app.tsx with this exact content:

```
import { TodoInput } from "@/components/todo-input"
import { TodoList } from "@/components/todo-list"
import { useTodos } from "@/hooks/use-todos"

export function TodoApp() {
  const { state, addTodo, toggleTodo, deleteTodo } = useTodos()

  return (
    <main className="min-h-screen bg-white px-4 pt-[max(2rem,env(safe-area-inset-top))] pb-[max(2rem,env(safe-area-inset-bottom))]">
      <div className="w-full max-w-md mx-auto flex flex-col gap-6">
        <h1 className="text-xl font-semibold text-gray-900">Tauri Todo</h1>
        <TodoInput onAdd={addTodo} disabled={state.status !== "ready"} />
        <TodoList state={state} onToggle={toggleTodo} onDelete={deleteTodo} />
      </div>
    </main>
  )
}
```

Critical:
- `py-8` is replaced, NOT supplemented. After this plan, `py-8` must not appear anywhere in this file.
- The Tailwind arbitrary values have no spaces inside the brackets: `pt-[max(2rem,env(safe-area-inset-top))]` — Tailwind v4 parses the arbitrary value as a single token; spaces would break class recognition. RESEARCH Pattern 4 gives the exact form.
- `max(2rem, env(...))` fallback preserves the 2rem floor on no-notch devices (Pitfall 5). If inset is 0, padding resolves to 2rem (same as old py-8). If inset is >2rem, padding uses the inset.
- No hover: classes added.
- No @monorepo-template/ui imports added.
- Preserve existing `@/components/todo-input`, `@/components/todo-list`, `@/hooks/use-todos` imports verbatim (alphabetical per perfectionist/sort-imports).
- H1 copy `"Tauri Todo"` preserved verbatim (UI-SPEC Copywriting Contract).

After the edit, run the three invariant tests to confirm UX-02 and UX-03 still hold (these tests exist in Plan 01 and must remain green after this refactor):

```
pnpm --filter @monorepo-template/tauri-todo test -- --run src/lint/no-hover.test.ts src/lint/no-ui-package.test.ts src/components/todo-app.test.tsx
```

If no-hover.test.ts fails, check for accidentally introduced `hover:` classes across this task + Task 1 + Task 2.
If no-ui-package.test.ts fails, check for accidentally introduced `@monorepo-template/ui` imports.

verify (automated):

pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-app.test.tsx src/lint/no-hover.test.ts src/lint/no-ui-package.test.ts

acceptance_criteria:
- `grep -F 'pt-[max(2rem,env(safe-area-inset-top))]' apps/tauri-todo/src/components/todo-app.tsx` returns 1.
- `grep -F 'pb-[max(2rem,env(safe-area-inset-bottom))]' apps/tauri-todo/src/components/todo-app.tsx` returns 1.
- `grep -c '\bpy-8\b' apps/tauri-todo/src/components/todo-app.tsx` returns 0.
- `grep -c 'min-h-screen' apps/tauri-todo/src/components/todo-app.tsx` returns 1 (preserved).
- `grep -c '\bbg-white\b' apps/tauri-todo/src/components/todo-app.tsx` returns 1 (preserved).
- `grep -c '\bpx-4\b' apps/tauri-todo/src/components/todo-app.tsx` returns 1 (preserved).
- `grep -c 'Tauri Todo' apps/tauri-todo/src/components/todo-app.tsx` returns 1 (copy preserved).
- `grep -c '\bhover:' apps/tauri-todo/src/components/todo-app.tsx` returns 0.
- `grep -c '@monorepo-template/ui' apps/tauri-todo/src/components/todo-app.tsx` returns 0.
- `pnpm --filter @monorepo-template/tauri-todo test -- --run src/components/todo-app.test.tsx src/lint/no-hover.test.ts src/lint/no-ui-package.test.ts` exits 0.

done: main has safe-area padding, py-8 removed, all other classes preserved. Plan 01 todo-app.test.tsx goes green. Plan 01 lint tests stay green (UX-02 / UX-03 invariants re-verified after the refactor).

---

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (net-new) | Class-string-only changes on existing React components. No new IPC, no new capability, no new data flow. WebView-layer CSS env(safe-area-inset-*) is unlocked by the pre-existing viewport-fit=cover meta (Phase 1 D-25). |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | T (Tampering) | Accidental introduction of hover: utilities during refactor | mitigate | Task 3 acceptance runs `src/lint/no-hover.test.ts` (Plan 01). Any hover: class anywhere in apps/tauri-todo/src fails the suite. |
| T-03-09 | T (Tampering) | Accidental introduction of @monorepo-template/ui imports during refactor | mitigate | Task 3 acceptance runs `src/lint/no-ui-package.test.ts` (Plan 01). Any such import fails the suite. |
| T-03-10 | I (Information Disclosure) | Classes embed user data | accept | All class strings are static literals. No template interpolation of user input into className. |

No new trust boundary introduced. Reference Plan 02 for the capability boundary and Plan 04 for the haptic IPC boundary.

## Verification

Run the three-task targeted suite plus the invariant lints:

    pnpm --filter @monorepo-template/tauri-todo test -- --run src/components src/lint

Expected: all of todo-input.test.tsx, todo-item.test.tsx, todo-app.test.tsx, no-hover.test.ts, no-ui-package.test.ts pass after this plan.

Then run `pnpm --filter @monorepo-template/tauri-todo typecheck && pnpm --filter @monorepo-template/tauri-todo lint` to confirm no TypeScript or ESLint errors.

Manual/visual verification (D-19 on-device) is delivered in Plan 05.

## Success Criteria

- Three component files modified as specified; no JSX structure changes.
- All four 44px controls delivered (input, Add button, toggle both branches, delete).
- Row padding (D-06) unchanged.
- Safe-area insets applied to main with max(2rem, ...) fallback.
- Zero hover: classes introduced anywhere in apps/tauri-todo/src.
- Zero @monorepo-template/ui imports introduced anywhere in apps/tauri-todo.
- All component tests + lint tests pass.
- typecheck + lint pass.

## Output

After completion, create .planning/phases/03-mobile-polish/03-03-SUMMARY.md summarizing:
- Exact class-string diffs per file (before/after).
- Checkmark glyph scaling decision (text-xs to text-sm) with rationale.
- Safe-area arbitrary-value form chosen and why the max() fallback was mandatory.
- Which Plan 01 tests flipped from red to green.
