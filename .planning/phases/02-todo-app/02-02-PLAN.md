---
phase: 02-todo-app
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/tauri-todo/src/components/todo-app.tsx
  - apps/tauri-todo/src/components/todo-input.tsx
  - apps/tauri-todo/src/components/todo-list.tsx
  - apps/tauri-todo/src/components/todo-item.tsx
  - apps/tauri-todo/src/app.tsx
  - apps/tauri-todo/src/components/verification-screen.tsx
autonomous: true
requirements: [TODO-01, TODO-02, TODO-03, TODO-04, PERS-02]
tags: [react, tauri, components, ui, tailwind]
must_haves:
  truths:
    - TodoApp component renders a heading Tauri Todo, a TodoInput form, and a TodoList (all three children visible when state is ready)
    - TodoInput has a text input with placeholder Add a todo... and a button labeled Add that submits the form
    - TodoInput button is disabled when parent passes disabled=true OR when trimmed text is empty
    - TodoInput clears the text state and re-focuses via autoFocus after successful add
    - TodoInput submits on Enter key (native form onSubmit behavior) AND on Add button click
    - TodoList renders a centered Loading... message when state.status is loading
    - TodoList renders a generic error message (Could not load todos. Restart the app and try again.) when state.status is error
    - TodoList renders the empty state (No todos yet / Add your first todo above) when state.status is ready and todos array length is 0
    - TodoList renders a <ul> of <TodoItem> rows when state.status is ready and todos length greater than 0
    - TodoItem renders a circular toggle button on the left, the todo text in the middle, and an X delete button on the right
    - TodoItem toggle button shows blue-700 filled circle with white checkmark when completed, white circle with gray-300 border when incomplete
    - TodoItem text has line-through plus gray-400 color when completed, gray-900 color when not
    - TodoItem toggle button has aria-label Mark as complete when incomplete, Mark as incomplete when completed
    - TodoItem delete button has aria-label Delete todo
    - app.tsx imports TodoApp from @/components/todo-app (NOT VerificationScreen) and renders it
    - verification-screen.tsx file is deleted (no longer exists after this plan)
    - All four new components use named exports (no default export) and named-plus-kebab-case file conventions
    - TypeScript typecheck passes with zero errors after the full set of component + wiring + delete changes
    - ESLint passes with zero errors
  artifacts:
    - path: apps/tauri-todo/src/components/todo-app.tsx
      provides: TodoApp container component
      exports: [TodoApp]
      contains: export function TodoApp
      min_lines: 14
    - path: apps/tauri-todo/src/components/todo-input.tsx
      provides: TodoInput form component
      exports: [TodoInput]
      contains: export function TodoInput
      min_lines: 30
    - path: apps/tauri-todo/src/components/todo-list.tsx
      provides: TodoList wrapper with discriminated-union branching
      exports: [TodoList]
      contains: export function TodoList
      min_lines: 35
    - path: apps/tauri-todo/src/components/todo-item.tsx
      provides: TodoItem single row component
      exports: [TodoItem]
      contains: export function TodoItem
      min_lines: 30
    - path: apps/tauri-todo/src/app.tsx
      provides: App entry now wrapping TodoApp
      contains: import { TodoApp } from "@/components/todo-app"
      must_not_contain: VerificationScreen
    - path: apps/tauri-todo/src/components/verification-screen.tsx
      provides: DELETED
      must_not_contain: FILE_DOES_NOT_EXIST
  key_links:
    - from: apps/tauri-todo/src/components/todo-app.tsx
      to: apps/tauri-todo/src/hooks/use-todos.ts
      via: "import { useTodos } from @/hooks/use-todos"
      pattern: useTodos hook consumption destructuring state and callbacks
    - from: apps/tauri-todo/src/components/todo-app.tsx
      to: TodoInput and TodoList
      via: JSX composition passing addTodo toggleTodo deleteTodo as props
      pattern: onAdd onToggle onDelete callback props drill
    - from: apps/tauri-todo/src/components/todo-list.tsx
      to: apps/tauri-todo/src/hooks/use-todos.ts
      via: import type TodosState and Todo
      pattern: type-only import for TodosState
    - from: apps/tauri-todo/src/app.tsx
      to: apps/tauri-todo/src/components/todo-app.tsx
      via: "import { TodoApp }"
      pattern: one-line swap from VerificationScreen to TodoApp
---

<objective>
Build the four presentational React components (TodoApp, TodoInput, TodoList, TodoItem), wire them into app.tsx via a one-line import swap, and delete the now-unused Phase 1 verification-screen.tsx.

Purpose: Delivers the full user-visible feature set (TODO-01 add, TODO-02 toggle, TODO-03 delete, TODO-04 empty state) and the rendering half of PERS-02 (reading persisted todos on cold start and showing them). The components are pure presentational — all state and I/O live in useTodos from Plan 01. This plan contains the entire UI delta from Phase 1 to Phase 2: all new component files, the app.tsx swap, and the deletion of the verification screen.

Output:
- 4 new component files under apps/tauri-todo/src/components/
- apps/tauri-todo/src/app.tsx updated to import and render TodoApp
- apps/tauri-todo/src/components/verification-screen.tsx deleted

DELETION ORDERING: Create all 4 new components first, then update app.tsx, then delete verification-screen.tsx LAST. This prevents a broken intermediate state where app.tsx imports a deleted file (RESEARCH Pitfall 4).
</objective>

<execution_context>
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-todo-app/02-CONTEXT.md
@.planning/phases/02-todo-app/02-RESEARCH.md
@.planning/phases/02-todo-app/02-PATTERNS.md
@.planning/phases/02-todo-app/02-UI-SPEC.md
@CLAUDE.md
@.agents/rules/typescript.md

@apps/tauri-todo/src/components/verification-screen.tsx
@apps/tauri-todo/src/app.tsx
@apps/tauri-todo/src/hooks/use-todos.ts
@apps/tauri-todo/src/main.tsx
@apps/tauri-todo/src/index.css
@apps/tauri-todo/tsconfig.json
@apps/tauri-todo/.planning/phases/02-todo-app/02-01-SUMMARY.md

<interfaces>
Contracts this plan consumes (from Plan 01 output) and provides (to downstream plans).

CONSUMED — from apps/tauri-todo/src/hooks/use-todos.ts (Plan 01):

Type exports:
- Todo = { id: string; text: string; completed: boolean }
- TodosState = { status: "loading" } | { status: "ready"; todos: Todo[] } | { status: "error"; message: string }

Function export useTodos returns an object with:
- state: TodosState
- addTodo: (text: string) => Promise<void>
- toggleTodo: (id: string) => Promise<void>
- deleteTodo: (id: string) => Promise<void>

PROVIDED — component prop contracts:

TodoApp: no props (self-contained, calls useTodos internally)

TodoInput props:
- onAdd: (text: string) => Promise<void>  (the hook's addTodo)
- disabled: boolean  (true when state.status is not ready)

TodoList props:
- state: TodosState
- onToggle: (id: string) => Promise<void>  (the hook's toggleTodo)
- onDelete: (id: string) => Promise<void>  (the hook's deleteTodo)

TodoItem props:
- todo: Todo
- onToggle: (id: string) => Promise<void>
- onDelete: (id: string) => Promise<void>

UI-SPEC VERBATIM TAILWIND CLASSES (02-UI-SPEC.md lines 104-169 + 02-RESEARCH.md lines 803-823):

Main container: min-h-screen bg-white px-4 py-8
Inner container: w-full max-w-md mx-auto flex flex-col gap-6
App heading: text-xl font-semibold text-gray-900
Form row: flex gap-2
Text input: flex-1 h-10 px-3 bg-white border border-gray-200 rounded-md text-base text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-700
Add button: shrink-0 h-10 px-4 bg-blue-700 text-white text-sm font-normal rounded-md active:opacity-90 disabled:opacity-50
Todo list ul: flex flex-col gap-2
Todo row li: flex items-center gap-3 px-3 py-3 rounded-lg bg-white border border-gray-200
Toggle incomplete: w-6 h-6 shrink-0 rounded-full border-2 border-gray-300 bg-white flex items-center justify-center active:opacity-90
Toggle completed: w-6 h-6 shrink-0 rounded-full bg-blue-700 border-2 border-blue-700 flex items-center justify-center active:opacity-90
Todo text normal: flex-1 text-base text-gray-900
Todo text completed: flex-1 text-base line-through text-gray-400
Delete button: w-8 h-8 shrink-0 flex items-center justify-center rounded-md text-gray-400 active:opacity-90 active:text-red-600
Loading container: py-12 flex items-center justify-center  with inner p: text-sm text-gray-500
Empty state container: py-12 flex flex-col items-center gap-2  with heading p: text-base font-semibold text-gray-900  and body p: text-sm text-gray-500

COPY STRINGS (02-UI-SPEC.md lines 196-209):
- App heading: "Tauri Todo"
- Input placeholder: "Add a todo..."
- Add button label: "Add"
- Empty state heading: "No todos yet"
- Empty state body: "Add your first todo above"
- Loading copy: "Loading..."
- Error copy: "Could not load todos. Restart the app and try again."
- Delete button glyph: "×" (unicode x2715 multiplication sign)
- Completed checkmark glyph: "✓" (unicode x2713 check mark)
- Delete button aria-label: "Delete todo"
- Toggle aria-label when incomplete: "Mark as complete"
- Toggle aria-label when completed: "Mark as incomplete"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create all 4 presentational components (TodoApp, TodoInput, TodoList, TodoItem)</name>

  <files>
    apps/tauri-todo/src/components/todo-app.tsx (NEW)
    apps/tauri-todo/src/components/todo-input.tsx (NEW)
    apps/tauri-todo/src/components/todo-list.tsx (NEW)
    apps/tauri-todo/src/components/todo-item.tsx (NEW)
  </files>

  <read_first>
    - apps/tauri-todo/src/components/verification-screen.tsx (layout shell at lines 64-67, form pattern at lines 70-85, button pattern at lines 98-104, discriminated-branching at lines 86-93; copy all Tailwind classes verbatim where they match UI-SPEC)
    - apps/tauri-todo/src/hooks/use-todos.ts (consume useTodos, Todo, TodosState — this MUST exist from Plan 01 before starting)
    - .planning/phases/02-todo-app/02-UI-SPEC.md (full visual contract — Tailwind classes, copy strings, layout structure)
    - .planning/phases/02-todo-app/02-RESEARCH.md (lines 618-776 — complete component code examples; lines 803-823 Tailwind reference table)
    - .planning/phases/02-todo-app/02-PATTERNS.md (per-file analog mappings — e.g., TodoInput mirrors verification-screen IPC form)
    - .agents/rules/typescript.md (import type rule, no-default-export rule)
    - CLAUDE.md (kebab-case files, named exports, no semicolons, 120-char width)
  </read_first>

  <behavior>
    TodoApp behavior:
    - Renders main with bg-white and min-height screen
    - Renders h1 "Tauri Todo"
    - Renders TodoInput with onAdd=addTodo and disabled=(state.status !== "ready")
    - Renders TodoList with state, onToggle=toggleTodo, onDelete=deleteTodo
    - Does NOT render any loading indicator itself (delegated to TodoList)

    TodoInput behavior:
    - Controlled input bound to local text state (useState)
    - autoFocus attribute on the input so keyboard opens on app launch
    - form element with onSubmit handler that calls e.preventDefault, trims text, no-ops if empty, awaits onAdd, clears text
    - Submit button disabled when (disabled || text.trim().length === 0)
    - Button type="submit" so Enter key submits

    TodoList behavior (early-return branching in order):
    - if state.status === "loading" return loading indicator
    - if state.status === "error" return error message with generic copy
    - if state.todos.length === 0 return empty-state panel
    - else return ul with TodoItem per todo, key=todo.id

    TodoItem behavior:
    - Renders li with row flex layout
    - Toggle button: left side, circular, bg and border depend on todo.completed
    - When completed: filled blue circle with white ✓ inside
    - When not completed: empty white circle with gray border
    - Text span: middle, flex-1, line-through + gray-400 when completed, gray-900 when not
    - Delete button: right side, renders × glyph, active:text-red-600
    - onClick handlers call onToggle(todo.id) and onDelete(todo.id) respectively
    - All buttons have type="button" (NOT submit) and proper aria-labels
  </behavior>

  <action>
    Create 4 component files in the exact order: todo-input.tsx, todo-item.tsx, todo-list.tsx, todo-app.tsx (leaf to root order — avoids any intermediate compile error where a parent references a not-yet-written child). Each file uses: no semicolons, double quotes, 120-char max, named export only, import type for type-only imports.

    FILE 1 — apps/tauri-todo/src/components/todo-input.tsx (copy verbatim between [CODE START] and [CODE END] markers):

    [CODE START]
    import { useState } from "react"

    type TodoInputProps = {
      onAdd: (text: string) => Promise<void>
      disabled: boolean
    }

    export function TodoInput({ onAdd, disabled }: TodoInputProps) {
      const [text, setText] = useState("")

      async function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        const trimmed = text.trim()
        if (trimmed.length === 0) return
        await onAdd(trimmed)
        setText("")
      }

      const canSubmit = !disabled && text.trim().length > 0

      return (
        <form className="flex gap-2" onSubmit={handleSubmit}>
          <input
            autoFocus
            className="flex-1 h-10 px-3 bg-white border border-gray-200 rounded-md text-base text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-700"
            onChange={(e) => setText(e.target.value)}
            placeholder="Add a todo..."
            value={text}
          />
          <button
            className="shrink-0 h-10 px-4 bg-blue-700 text-white text-sm font-normal rounded-md active:opacity-90 disabled:opacity-50"
            disabled={!canSubmit}
            type="submit"
          >
            Add
          </button>
        </form>
      )
    }
    [CODE END]

    FILE 2 — apps/tauri-todo/src/components/todo-item.tsx (copy verbatim between markers):

    [CODE START]
    import type { Todo } from "@/hooks/use-todos"

    type TodoItemProps = {
      todo: Todo
      onToggle: (id: string) => Promise<void>
      onDelete: (id: string) => Promise<void>
    }

    export function TodoItem({ todo, onToggle, onDelete }: TodoItemProps) {
      const toggleClass = todo.completed
        ? "w-6 h-6 shrink-0 rounded-full bg-blue-700 border-2 border-blue-700 flex items-center justify-center active:opacity-90"
        : "w-6 h-6 shrink-0 rounded-full border-2 border-gray-300 bg-white flex items-center justify-center active:opacity-90"

      const textClass = todo.completed
        ? "flex-1 text-base line-through text-gray-400"
        : "flex-1 text-base text-gray-900"

      return (
        <li className="flex items-center gap-3 px-3 py-3 rounded-lg bg-white border border-gray-200">
          <button
            aria-label={todo.completed ? "Mark as incomplete" : "Mark as complete"}
            className={toggleClass}
            onClick={() => onToggle(todo.id)}
            type="button"
          >
            {todo.completed && <span className="text-white text-xs">✓</span>}
          </button>
          <span className={textClass}>{todo.text}</span>
          <button
            aria-label="Delete todo"
            className="w-8 h-8 shrink-0 flex items-center justify-center rounded-md text-gray-400 active:opacity-90 active:text-red-600"
            onClick={() => onDelete(todo.id)}
            type="button"
          >
            <span className="text-xl leading-none">×</span>
          </button>
        </li>
      )
    }
    [CODE END]

    FILE 3 — apps/tauri-todo/src/components/todo-list.tsx (copy verbatim between markers):

    [CODE START]
    import { TodoItem } from "@/components/todo-item"
    import type { TodosState } from "@/hooks/use-todos"

    type TodoListProps = {
      state: TodosState
      onToggle: (id: string) => Promise<void>
      onDelete: (id: string) => Promise<void>
    }

    export function TodoList({ state, onToggle, onDelete }: TodoListProps) {
      if (state.status === "loading") {
        return (
          <div className="py-12 flex items-center justify-center">
            <p className="text-sm text-gray-500">Loading...</p>
          </div>
        )
      }

      if (state.status === "error") {
        return (
          <div className="py-12 flex items-center justify-center">
            <p className="text-sm text-red-600">Could not load todos. Restart the app and try again.</p>
          </div>
        )
      }

      if (state.todos.length === 0) {
        return (
          <div className="py-12 flex flex-col items-center gap-2">
            <p className="text-base font-semibold text-gray-900">No todos yet</p>
            <p className="text-sm text-gray-500">Add your first todo above</p>
          </div>
        )
      }

      return (
        <ul className="flex flex-col gap-2">
          {state.todos.map((todo) => (
            <TodoItem key={todo.id} todo={todo} onToggle={onToggle} onDelete={onDelete} />
          ))}
        </ul>
      )
    }
    [CODE END]

    FILE 4 — apps/tauri-todo/src/components/todo-app.tsx (copy verbatim between markers):

    [CODE START]
    import { TodoInput } from "@/components/todo-input"
    import { TodoList } from "@/components/todo-list"
    import { useTodos } from "@/hooks/use-todos"

    export function TodoApp() {
      const { state, addTodo, toggleTodo, deleteTodo } = useTodos()

      return (
        <main className="min-h-screen bg-white px-4 py-8">
          <div className="w-full max-w-md mx-auto flex flex-col gap-6">
            <h1 className="text-xl font-semibold text-gray-900">Tauri Todo</h1>
            <TodoInput onAdd={addTodo} disabled={state.status !== "ready"} />
            <TodoList state={state} onToggle={toggleTodo} onDelete={deleteTodo} />
          </div>
        </main>
      )
    }
    [CODE END]

    Write each file's content exactly as it appears between its [CODE START] and [CODE END] markers. Do NOT include the markers in the written file. Include a trailing newline at end of each file.

    DO NOT:
    - Import from @monorepo-template/ui (explicitly excluded per Phase 1 D-14, UI-SPEC line 22-23)
    - Use hover: Tailwind variants anywhere (mobile-only; use active: for touch feedback)
    - Use default exports anywhere
    - Add return type annotations on components (exempt per .agents/rules/typescript.md line 114-131)
    - Use any type anywhere
    - Import clsx, cn, class-variance-authority — raw Tailwind string ternaries only
    - Create any CSS files (Tailwind utilities are sufficient)
    - Add test files (nyquist_validation: false per RESEARCH line 90)
    - Add confirmation dialog on delete (D-14 + UI-SPEC line 185 — immediate delete, no confirm)
    - Add loading spinner to Add button (D-15-style minimalism; button is just disabled during load)
    - Use string concatenation for className (use ternary or static strings)
    - Add State switch statements — the early-return if chain is the chosen pattern (RESEARCH lines 321-356)

    ORDER OF FILE CREATION (leaf-first, prevents compile errors):
    1. Create todo-input.tsx first (no internal imports)
    2. Create todo-item.tsx second (only type imports from use-todos.ts)
    3. Create todo-list.tsx third (imports TodoItem and type from use-todos.ts)
    4. Create todo-app.tsx last (imports TodoInput, TodoList, useTodos)
  </action>

  <verify>
    <automated>test -f apps/tauri-todo/src/components/todo-input.tsx && test -f apps/tauri-todo/src/components/todo-item.tsx && test -f apps/tauri-todo/src/components/todo-list.tsx && test -f apps/tauri-todo/src/components/todo-app.tsx && echo "ALL FILES EXIST"</automated>
    <automated>cd apps/tauri-todo && pnpm typecheck 2>&1 | tee /tmp/components-typecheck.log; grep -q "error TS" /tmp/components-typecheck.log && echo "TYPECHECK FAILED" || echo "TYPECHECK PASSED"</automated>
    <automated>cd apps/tauri-todo && pnpm lint 2>&1 | tee /tmp/components-lint.log; (grep -E "(error|problem)" /tmp/components-lint.log | grep -v "0 errors" && echo "LINT FAILED") || echo "LINT PASSED"</automated>
  </verify>

  <acceptance_criteria>
    All 4 component files exist and contain required content:

    - test -f apps/tauri-todo/src/components/todo-app.tsx
    - test -f apps/tauri-todo/src/components/todo-input.tsx
    - test -f apps/tauri-todo/src/components/todo-list.tsx
    - test -f apps/tauri-todo/src/components/todo-item.tsx

    Named exports present:
    - grep -q "^export function TodoApp" apps/tauri-todo/src/components/todo-app.tsx
    - grep -q "^export function TodoInput" apps/tauri-todo/src/components/todo-input.tsx
    - grep -q "^export function TodoList" apps/tauri-todo/src/components/todo-list.tsx
    - grep -q "^export function TodoItem" apps/tauri-todo/src/components/todo-item.tsx

    No default exports anywhere:
    - ! grep -l "^export default" apps/tauri-todo/src/components/todo-*.tsx

    No any, no hover:
    - ! grep -E ": any(\W|$)" apps/tauri-todo/src/components/todo-*.tsx
    - ! grep -E "hover:" apps/tauri-todo/src/components/todo-*.tsx
    - ! grep -E "@monorepo-template/ui" apps/tauri-todo/src/components/todo-*.tsx

    Import type used for type-only imports:
    - grep -q '^import type { Todo }' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q '^import type { TodosState }' apps/tauri-todo/src/components/todo-list.tsx

    TodoApp uses useTodos and renders both children:
    - grep -q 'import { useTodos } from "@/hooks/use-todos"' apps/tauri-todo/src/components/todo-app.tsx
    - grep -q '<TodoInput onAdd={addTodo}' apps/tauri-todo/src/components/todo-app.tsx
    - grep -q '<TodoList state={state}' apps/tauri-todo/src/components/todo-app.tsx
    - grep -q 'Tauri Todo' apps/tauri-todo/src/components/todo-app.tsx

    TodoInput has form, input with autoFocus and placeholder, submit button:
    - grep -q 'onSubmit={handleSubmit}' apps/tauri-todo/src/components/todo-input.tsx
    - grep -q 'autoFocus' apps/tauri-todo/src/components/todo-input.tsx
    - grep -q 'placeholder="Add a todo..."' apps/tauri-todo/src/components/todo-input.tsx
    - grep -q 'type="submit"' apps/tauri-todo/src/components/todo-input.tsx
    - grep -q 'e.preventDefault()' apps/tauri-todo/src/components/todo-input.tsx
    - grep -q 'setText("")' apps/tauri-todo/src/components/todo-input.tsx

    TodoList has all 4 branches: loading, error, empty, populated:
    - grep -q 'state.status === "loading"' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'state.status === "error"' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'state.todos.length === 0' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'Loading...' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'Could not load todos' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'No todos yet' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'Add your first todo above' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'state.todos.map' apps/tauri-todo/src/components/todo-list.tsx
    - grep -q 'key={todo.id}' apps/tauri-todo/src/components/todo-list.tsx

    TodoItem has toggle button, text span, delete button, and aria labels:
    - grep -q 'aria-label={todo.completed ? "Mark as incomplete" : "Mark as complete"}' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'aria-label="Delete todo"' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'onClick={() => onToggle(todo.id)}' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'onClick={() => onDelete(todo.id)}' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'type="button"' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'line-through' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'bg-blue-700' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q 'active:text-red-600' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q '×' apps/tauri-todo/src/components/todo-item.tsx
    - grep -q '✓' apps/tauri-todo/src/components/todo-item.tsx

    TodoApp Tailwind layout classes present:
    - grep -q 'min-h-screen bg-white px-4 py-8' apps/tauri-todo/src/components/todo-app.tsx
    - grep -q 'w-full max-w-md mx-auto flex flex-col gap-6' apps/tauri-todo/src/components/todo-app.tsx
    - grep -q 'text-xl font-semibold text-gray-900' apps/tauri-todo/src/components/todo-app.tsx

    All files pass typecheck and lint:
    - cd apps/tauri-todo && pnpm typecheck exits 0 with no "error TS" output
    - cd apps/tauri-todo && pnpm lint exits 0 with no errors
  </acceptance_criteria>

  <done>
    Four component files exist in apps/tauri-todo/src/components/ with the exact content specified. All grep acceptance criteria pass. TypeScript typecheck and ESLint both pass with zero errors. Components are pure presentational — they consume Plan 01's useTodos hook for state. Note: typecheck MAY still fail at this point if app.tsx still imports VerificationScreen AND verification-screen.tsx has NOT been deleted. Verify the typecheck after Task 2 completes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap app.tsx import from VerificationScreen to TodoApp, then delete verification-screen.tsx</name>

  <files>
    apps/tauri-todo/src/app.tsx (MODIFY — swap import and JSX, one-line change)
    apps/tauri-todo/src/components/verification-screen.tsx (DELETE — remove file from filesystem and git)
  </files>

  <read_first>
    - apps/tauri-todo/src/app.tsx (current 5-line file — must read to confirm exact import statement)
    - apps/tauri-todo/src/components/verification-screen.tsx (file to be deleted — confirm it exists and has no other consumers)
    - apps/tauri-todo/src/main.tsx (confirm main.tsx imports App from ./app — so App name must stay the same)
    - apps/tauri-todo/src/components/todo-app.tsx (confirm it was created in Task 1 and exports TodoApp)
    - .planning/phases/02-todo-app/02-RESEARCH.md (Pitfall 4 — update import BEFORE deleting to avoid intermediate broken state)
  </read_first>

  <behavior>
    - Test 1: After app.tsx edit, the file contains "import { TodoApp } from \"@/components/todo-app\"" and renders <TodoApp />
    - Test 2: After app.tsx edit, the file does NOT mention VerificationScreen anywhere
    - Test 3: The exported App function still has the same name "App" (main.tsx's import must still resolve)
    - Test 4: After verification-screen.tsx deletion, the file does not exist on filesystem (ls returns exit code 2 for that specific path)
    - Test 5: After deletion, git status shows verification-screen.tsx as deleted
    - Test 6: TypeScript typecheck passes with zero errors
    - Test 7: ESLint passes with zero errors
    - Test 8: grep across the entire apps/tauri-todo/src/ tree finds ZERO remaining references to VerificationScreen or verification-screen
  </behavior>

  <action>
    ORDER OF OPERATIONS (critical — RESEARCH Pitfall 4):
    1. FIRST: Edit app.tsx to swap the import
    2. SECOND: Delete verification-screen.tsx

    If deletion happens first, the project is in a broken state where app.tsx imports a file that does not exist.

    STEP 1 — Edit apps/tauri-todo/src/app.tsx:

    Use the Edit tool. The current 5-line file content is:

    [CURRENT CONTENT START]
    import { VerificationScreen } from "@/components/verification-screen"

    export function App() {
      return <VerificationScreen />
    }
    [CURRENT CONTENT END]

    Replace the entire file with this 5-line content (same structure, swapped component name):

    [NEW CONTENT START]
    import { TodoApp } from "@/components/todo-app"

    export function App() {
      return <TodoApp />
    }
    [NEW CONTENT END]

    Use the Edit tool:
    - old_string: the 5 lines between [CURRENT CONTENT START] and [CURRENT CONTENT END] markers
    - new_string: the 5 lines between [NEW CONTENT START] and [NEW CONTENT END] markers

    The function name "App" MUST stay exactly "App" because main.tsx (verified in read_first) imports it by name: `import { App } from "@/app"`.

    STEP 2 — Delete apps/tauri-todo/src/components/verification-screen.tsx:

    Use the Bash tool to delete the file. Use git rm to stage the deletion so it is tracked properly:

    Command: `git rm apps/tauri-todo/src/components/verification-screen.tsx`

    Run from the repo root. This removes the file from the working tree AND stages the deletion for the next commit. Do NOT use plain `rm` — git rm ensures the deletion is tracked.

    If git rm refuses (e.g., if the file has unstaged modifications), fall back to:
    1. `rm apps/tauri-todo/src/components/verification-screen.tsx` (delete from filesystem)
    2. The git commit at the end of plan execution will record the deletion.

    STEP 3 — Verify no lingering references:

    Run: `grep -rn "VerificationScreen\|verification-screen" apps/tauri-todo/src/ 2>/dev/null`

    This MUST return empty (exit code 1 for grep, meaning no matches). If any match is found, investigate — there should be no references after Steps 1 and 2.

    DO NOT:
    - Delete verification-screen.tsx before updating app.tsx (creates broken intermediate state per RESEARCH Pitfall 4)
    - Rename the App function or change main.tsx (main.tsx line reads `import { App } from "@/app"` — keep the name)
    - Keep verification-screen.tsx "just in case" (D-08 explicit: remove Phase 1 verification screen, served its purpose)
    - Leave any commented-out code, TODO markers, or reference to VerificationScreen in app.tsx
    - Delete other Phase 1 artifacts (mobile.json, Cargo.toml, lib.rs beyond Plan 01's scope, etc.)
  </action>

  <verify>
    <automated>test -f apps/tauri-todo/src/app.tsx && grep -q 'import { TodoApp } from "@/components/todo-app"' apps/tauri-todo/src/app.tsx && echo "APP.TSX UPDATED" || echo "APP.TSX NOT UPDATED"</automated>
    <automated>! test -f apps/tauri-todo/src/components/verification-screen.tsx && echo "VERIFICATION-SCREEN DELETED" || echo "VERIFICATION-SCREEN STILL EXISTS"</automated>
    <automated>! grep -rn "VerificationScreen\|verification-screen" apps/tauri-todo/src/ 2>/dev/null && echo "NO RESIDUAL REFERENCES" || echo "RESIDUAL REFERENCES FOUND"</automated>
    <automated>cd apps/tauri-todo && pnpm typecheck 2>&1 | tee /tmp/final-typecheck.log; grep -q "error TS" /tmp/final-typecheck.log && echo "TYPECHECK FAILED" || echo "TYPECHECK PASSED"</automated>
    <automated>cd apps/tauri-todo && pnpm lint 2>&1 | tee /tmp/final-lint.log; (grep -E "(error|problem)" /tmp/final-lint.log | grep -v "0 errors" && echo "LINT FAILED") || echo "LINT PASSED"</automated>
    <automated>cd apps/tauri-todo && pnpm build 2>&1 | tee /tmp/final-build.log; grep -E "^(error|Error)" /tmp/final-build.log && echo "BUILD FAILED" || echo "BUILD PASSED"</automated>
  </verify>

  <acceptance_criteria>
    app.tsx state:
    - grep -q 'import { TodoApp } from "@/components/todo-app"' apps/tauri-todo/src/app.tsx
    - grep -q 'return <TodoApp />' apps/tauri-todo/src/app.tsx
    - grep -q '^export function App()' apps/tauri-todo/src/app.tsx
    - ! grep -q "VerificationScreen" apps/tauri-todo/src/app.tsx
    - ! grep -q "verification-screen" apps/tauri-todo/src/app.tsx
    - File has exactly 5 non-empty lines: test $(grep -c . apps/tauri-todo/src/app.tsx) -eq 3  (imports, export function, return line; 5 total lines with 2 blank counts as 3 non-empty)

    verification-screen deletion:
    - ! test -f apps/tauri-todo/src/components/verification-screen.tsx
    - git status shows verification-screen.tsx as deleted: git status --short apps/tauri-todo/src/components/verification-screen.tsx prints "D " or " D"

    Zero residual references across the whole src tree:
    - ! grep -rn "VerificationScreen" apps/tauri-todo/src/ 2>/dev/null
    - ! grep -rn "verification-screen" apps/tauri-todo/src/ 2>/dev/null

    Full-project build health (the real test of this wave):
    - cd apps/tauri-todo && pnpm typecheck exits 0 with no "error TS"
    - cd apps/tauri-todo && pnpm lint exits 0 with no errors
    - cd apps/tauri-todo && pnpm build exits 0 with no build errors (Vite build succeeds; dist/ produced)

    main.tsx still imports App (sanity check the App function name wasn't accidentally changed):
    - grep -q 'import { App }' apps/tauri-todo/src/main.tsx
  </acceptance_criteria>

  <done>
    app.tsx imports TodoApp and renders it. verification-screen.tsx is deleted from filesystem and git. Zero residual references to the old component anywhere in apps/tauri-todo/src/. TypeScript typecheck, ESLint, and Vite production build all succeed. main.tsx still resolves its `import { App } from "@/app"` because the App export name is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to React state | User-typed todo text enters input via controlled onChange; React escapes automatically on render |
| JSX render to DOM | React's default JSX interpolation is XSS-safe — {todo.text} renders text content, never HTML |
| Component props to child components | No untrusted input traverses component boundaries (all props are either local state or hook callbacks) |

All boundaries live inside the WebView sandbox. No network, no innerHTML, no dangerouslySetInnerHTML.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | XSS via todo text in TodoItem | mitigate | React auto-escapes JSX expression interpolation. {todo.text} and {toggleClass} and {textClass} are all safe. Zero dangerouslySetInnerHTML anywhere. Verified: grep for dangerouslySetInnerHTML returns empty across all 4 components. |
| T-02-08 | Information disclosure | Error state message rendering | mitigate | TodoList error branch uses generic copy "Could not load todos. Restart the app and try again." — does NOT render state.message directly. The raw err.message from the hook is held in state but not shown; the UI displays only the generic line (UI-SPEC line 207). Decision verified in Task 1 action. |
| T-02-09 | Denial of service | Infinite render loops from bad dependency arrays | mitigate | TodoInput useState is self-contained. TodoApp calls useTodos once; the hook's effect has empty dep array (Plan 01 verified). No memoization bombs in the components. |
| T-02-10 | Elevation of privilege | Component importing from forbidden module | mitigate | Explicit exclusion: no `import from "@monorepo-template/ui"` allowed (UI-SPEC line 22-23, D-14). Verified via acceptance_criteria grep. |
| T-02-11 | Spoofing | N/A | accept | No authentication, no impersonation surface. |
| T-02-12 | Repudiation | N/A | accept | No audit log required. Single user on a single device. |
</threat_model>

<verification>
## Plan-Level Verification

After both tasks complete:

1. cd apps/tauri-todo && pnpm typecheck exits 0 (validates all new component types + hook consumption + app.tsx wiring)
2. cd apps/tauri-todo && pnpm lint exits 0 (validates conventions: named exports, import ordering, import type usage, no any, no default)
3. cd apps/tauri-todo && pnpm build exits 0 (validates Vite production build — catches any missing exports, stale imports, or module resolution failures)
4. grep -rn "VerificationScreen" apps/tauri-todo/src/ returns no matches (zero residual references to the deleted component)
5. All 4 new component files exist with expected exports
6. verification-screen.tsx is deleted (both filesystem and git)

## Manual Sanity Check (deferred to Plan 03's Android checkpoint)

No automated test runs the UI. Plan 03 (Android device verification checkpoint) will exercise:
- Cold-start load shows Loading... briefly, then either populated list (if store has data) or empty state
- Typing and tapping Add creates a new todo that appears in the list
- Tapping toggle flips completed styling
- Tapping delete removes the row
- Closing and reopening the app preserves the todos

If any of these fail on device, gap closure will be routed back through this plan.
</verification>

<success_criteria>
Plan 02 complete when:
1. All 4 new component files exist with specified content
2. app.tsx updated to import TodoApp
3. verification-screen.tsx deleted
4. All grep acceptance criteria from both tasks pass
5. typecheck, lint, and Vite build all exit 0
6. Zero residual references to VerificationScreen in apps/tauri-todo/src/

Requirements addressed:
- TODO-01 (add todo): TodoInput form calls onAdd=addTodo; input clears after success
- TODO-02 (toggle complete): TodoItem toggle button calls onToggle=toggleTodo
- TODO-03 (delete todo): TodoItem delete button calls onDelete=deleteTodo
- TODO-04 (empty state): TodoList renders "No todos yet" when state.todos.length === 0
- PERS-02 (persistence UI-side): TodoApp consumes useTodos which loads from store.json on mount (from Plan 01)
</success_criteria>

<output>
After completion, create .planning/phases/02-todo-app/02-02-SUMMARY.md describing:
- 4 components created (file paths + line counts)
- app.tsx swap complete
- verification-screen.tsx deletion confirmed
- Full typecheck + lint + build passing
- Ready for Android device verification in Plan 03
</output>
