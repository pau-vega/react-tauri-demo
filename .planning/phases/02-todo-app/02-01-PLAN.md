---
phase: 02-todo-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tauri-todo/src/hooks/use-todos.ts
  - apps/tauri-todo/src-tauri/src/lib.rs
autonomous: true
requirements: [TODO-01, TODO-02, TODO-03, PERS-01, PERS-02, PERS-03]
tags: [tauri, react, hooks, persistence, rust]
must_haves:
  truths:
    - useTodos hook loads persisted todos from store.json on mount via load with autoSave false and defaults empty
    - useTodos exposes addTodo toggleTodo deleteTodo callbacks that mutate state and call store.save after each mutation
    - useTodos exposes a discriminated-union TodosState with loading or ready-plus-todos or error-plus-message
    - Todo type is exported with fields id string text string completed boolean only
    - crypto.randomUUID is used for new todo IDs inside addTodo not in render
    - lib.rs no longer contains greet function or invoke_handler call but retains tauri_plugin_store plugin registration
    - Rust project compiles cargo check with zero warnings after greet removal
    - TypeScript typechecks with zero errors for the hook file
  artifacts:
    - path: apps/tauri-todo/src/hooks/use-todos.ts
      provides: useTodos hook Todo type TodosState type
      exports: [useTodos, Todo, TodosState]
      contains: export function useTodos
      min_lines: 60
    - path: apps/tauri-todo/src-tauri/src/lib.rs
      provides: Tauri v2 mobile entry with store plugin only
      contains: tauri_plugin_store
      must_not_contain: fn greet
  key_links:
    - from: apps/tauri-todo/src/hooks/use-todos.ts
      to: "@tauri-apps/plugin-store"
      via: named import of load and type Store
      pattern: import load type Store from plugin-store
    - from: apps/tauri-todo/src/hooks/use-todos.ts
      to: store.json
      via: load store.json with autoSave false and defaults empty
      pattern: load store.json autoSave false
    - from: apps/tauri-todo/src-tauri/src/lib.rs
      to: tauri_plugin_store
      via: builder chain plugin tauri_plugin_store Builder new build
      pattern: tauri_plugin_store Builder new build
---

<objective>
Build the useTodos custom hook (the single source of truth for todo state and persistence) and clean up the Phase 1 greet Rust command from lib.rs. This plan produces the data layer that Plan 02 UI components consume.

Purpose: Provides CRUD operations (add toggle delete) backed by the Tauri Store plugin (PERS-02) and removes the now-unused Rust greet command (D-09). The hook encapsulates all store I/O so presentational components stay pure. Rust cleanup happens here because it shares no files with Plan 02 UI work and the lib.rs edit is tiny, keeping this plan a single cohesive data-plus-backend-cleanup unit.

Output:
- apps/tauri-todo/src/hooks/use-todos.ts (hook plus Todo plus TodosState exports)
- apps/tauri-todo/src-tauri/src/lib.rs (greet command removed, store plugin registration retained)
</objective>

<execution_context>
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/Documents/base-monorepo-template/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-todo-app/02-CONTEXT.md
@.planning/phases/02-todo-app/02-RESEARCH.md
@.planning/phases/02-todo-app/02-PATTERNS.md
@CLAUDE.md
@.agents/rules/typescript.md

@apps/tauri-todo/src/components/verification-screen.tsx
@apps/tauri-todo/src-tauri/src/lib.rs
@apps/tauri-todo/src-tauri/capabilities/mobile.json
@packages/ui/src/hooks/use-mobile.ts
@apps/tauri-todo/package.json
@apps/tauri-todo/src-tauri/Cargo.toml
@apps/tauri-todo/tsconfig.json

<interfaces>
Key types and contracts the executor needs. No codebase exploration required.

From @tauri-apps/plugin-store (v2.4.2), verified against plugins-workspace/v2/plugins/store/guest-js/index.ts:

StoreOptions type fields:
- defaults - required object of key string to unknown
- autoSave - optional boolean or number, default is 100ms debounce, pass false to disable
- serializeFnName - optional string
- deserializeFnName - optional string
- createNew - optional boolean
- overrideDefaults - optional boolean

load function signature:
async function load(path string, options optional StoreOptions) returns Promise of Store
Store.load(path, options) is identical, load() delegates to it

Store class methods:
- async set(key string, value unknown) returns Promise void
- async get generic T (key string) returns Promise of T or undefined. IMPORTANT: undefined NOT null
- async save() returns Promise void
- async delete(key string) returns Promise boolean
- async clear() returns Promise void
- async has(key string) returns Promise boolean
- async keys() returns Promise of string array

From existing verification-screen.tsx (lines 5-15, 37-40, 53-61), the store I/O pattern to mirror:
- Discriminated-union state type with status idle or loading or success or error
- Load plus set plus save plus get pattern (Phase 1 verified on Android): call load with store.json and options, then call store.set with a key and value, then await store.save, then call store.get with a typed generic key
- Error narrowing pattern: catch err, narrow with err instanceof Error to err.message else String(err), then setState with status error and the message

From packages/ui/src/hooks/use-mobile.ts (lines 5-16), the only hook template in the repo (sync pattern). Mirror the empty-dep-array plus cleanup shape:
- React.useEffect with empty dep array
- Return cleanup function from effect body
- Use React.useState to track output
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useTodos hook with discriminated-union state and store-backed CRUD</name>

  <files>
    apps/tauri-todo/src/hooks/use-todos.ts (NEW; directory apps/tauri-todo/src/hooks/ does not exist yet, Write tool will create it)
  </files>

  <read_first>
    - apps/tauri-todo/src/components/verification-screen.tsx (store API usage pattern, error narrowing pattern, lines 5-15, 37-40, 43-62)
    - packages/ui/src/hooks/use-mobile.ts (hook shape, useState plus useEffect plus cleanup with empty dep array)
    - apps/tauri-todo/package.json (confirm plugin-store 2.4.2 is present)
    - apps/tauri-todo/src-tauri/capabilities/mobile.json (confirm store default permission granted)
    - apps/tauri-todo/tsconfig.json (confirm @/* alias maps to ./src/*)
    - .agents/rules/typescript.md (import type rules, never-any rule, discriminated-union rule)
    - .planning/phases/02-todo-app/02-CONTEXT.md (locked decisions D-01 through D-15)
    - .planning/phases/02-todo-app/02-RESEARCH.md (Pattern 1 reference implementation, Pitfalls 1-8)
  </read_first>

  <behavior>
    Test cases (verified via typecheck plus manual Android device test in Plan 03):
    - Test 1: On mount hook state starts as status loading
    - Test 2: After successful Store.load, hook state transitions to status ready with todos empty array when store is empty
    - Test 3: After successful Store.load, hook state transitions to status ready with todos array when todos key already exists
    - Test 4: addTodo with valid text appends a new Todo with fresh uuid, text, completed false to state.todos AND calls store.set for todos key with next array and then store.save
    - Test 5: addTodo with whitespace-only text is a no-op, state unchanged, no save call
    - Test 6: addTodo with leading or trailing whitespace trims before creating the todo
    - Test 7: toggleTodo(id) flips the completed field on the matching todo only, leaves others unchanged
    - Test 8: deleteTodo(id) removes the matching todo, leaves others unchanged
    - Test 9: All three mutations no-op when state.status is not ready
    - Test 10: If Store.load throws, state transitions to status error with message from err
    - Test 11: If store.save throws, state transitions to status error with message from err
    - Test 12: Under React StrictMode double-mount, the effect sets cancelled true in cleanup and checks the flag after each await to prevent racing updates
    - Test 13: The Store instance is held in useRef of Store or null NOT useState (verified by grep for useRef)
  </behavior>

  <action>
    Create the file apps/tauri-todo/src/hooks/use-todos.ts with the EXACT code below. The hooks/ directory does not exist yet; using the Write tool will create it automatically.

    CRITICAL FORMATTING RULES (enforced by Prettier plus ESLint):
    - NO semicolons at end of statements (.prettierrc has semi false)
    - 120-character line width max
    - Double quotes for strings
    - Top-level import type for type-only imports (the combined `import { load, type Store } from ...` form is allowed and expected)
    - Named imports sorted alphabetically (perfectionist/sort-imports)
    - NO default export, named exports only
    - NO any, use unknown with narrowing or generics
    - NO hover Tailwind variants (N/A here)

    Full file content to write, copy verbatim (no semicolons, double quotes, 120-char max):

    [CODE START]
    import { load, type Store } from "@tauri-apps/plugin-store"
    import { useEffect, useRef, useState } from "react"

    export type Todo = {
      id: string
      text: string
      completed: boolean
    }

    export type TodosState =
      | { status: "loading" }
      | { status: "ready"; todos: Todo[] }
      | { status: "error"; message: string }

    export function useTodos() {
      const [state, setState] = useState<TodosState>({ status: "loading" })
      const storeRef = useRef<Store | null>(null)

      useEffect(() => {
        let cancelled = false
        async function init() {
          try {
            const store = await load("store.json", { autoSave: false, defaults: {} })
            if (cancelled) return
            storeRef.current = store
            const stored = await store.get<Todo[]>("todos")
            if (cancelled) return
            setState({ status: "ready", todos: stored ?? [] })
          } catch (err) {
            if (cancelled) return
            const message = err instanceof Error ? err.message : String(err)
            setState({ status: "error", message })
          }
        }
        void init()
        return () => {
          cancelled = true
        }
      }, [])

      async function save(next: Todo[]) {
        const store = storeRef.current
        if (!store) return
        try {
          await store.set("todos", next)
          await store.save()
          setState({ status: "ready", todos: next })
        } catch (err) {
          const message = err instanceof Error ? err.message : String(err)
          setState({ status: "error", message })
        }
      }

      async function addTodo(text: string) {
        if (state.status !== "ready") return
        const trimmed = text.trim()
        if (trimmed.length === 0) return
        const next: Todo[] = [
          ...state.todos,
          { id: crypto.randomUUID(), text: trimmed, completed: false },
        ]
        await save(next)
      }

      async function toggleTodo(id: string) {
        if (state.status !== "ready") return
        const next = state.todos.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))
        await save(next)
      }

      async function deleteTodo(id: string) {
        if (state.status !== "ready") return
        const next = state.todos.filter((t) => t.id !== id)
        await save(next)
      }

      return { state, addTodo, toggleTodo, deleteTodo }
    }
    [CODE END]

    Write the content between [CODE START] and [CODE END] markers (exclusive of the markers themselves) into the file. Include a trailing newline after the final closing brace.

    DO NOT:
    - Add an isTauriRuntime guard (RESEARCH Open Question 2 resolved: let try-catch surface the error naturally, shows as error state in UI during web dev)
    - Use useState to hold the Store instance (must be useRef; StrictMode double-mount would trigger re-renders)
    - Use any anywhere (TypeScript strict plus .agents/rules/typescript.md line 145 bans it)
    - Treat store.get as returning null (it returns undefined, verified in source line 244)
    - Omit defaults empty object from the load options (TS type declares defaults required, omitting causes type error)
    - Omit autoSave false from the load options (default is true with 100ms debounce; D-05 requires explicit save)
    - Call crypto.randomUUID in a component body or at the top level (must be inside the mutation handler; otherwise new ID every render)
    - Add a return type annotation on useTodos (custom hooks are exempt per .agents/rules/typescript.md line 114-131)

    Why every line:
    - `import { load, type Store }`: named load function (verified consistent with Phase 1 verification-screen pattern); Store type-only for the ref annotation (cleaner than Awaited ReturnType of typeof load)
    - `useRef<Store | null>(null)`: RESEARCH Anti-Pattern, Store ref must not trigger re-renders (it is not display data); persists across StrictMode double-mount cleanly
    - `let cancelled = false` plus cleanup that sets `cancelled = true`: RESEARCH Pitfall 3, prevents StrictMode racing; checked after every await
    - `load("store.json", { autoSave: false, defaults: {} })`: matches verification-screen.tsx line 53 exactly; D-04 (same store file), D-05 (explicit save), RESEARCH Pitfall 7 (defaults required at type-check time)
    - `stored ?? []`: RESEARCH Pitfall 1 and 2, store.get returns undefined on first run, null-coalesce gives the expected empty array
    - `state.status !== "ready"` guards: prevents mutations before load completes; state is not a discriminated member of the ready branch until narrowed
    - `trimmed.length === 0` guards: matches UI-SPEC line 179-180 (Add button disabled when input is empty) and D-11 (input clears after adding; empty input should no-op, not add blank todos)
    - `err instanceof Error ? err.message : String(err)`: mirrors verification-screen.tsx lines 38-39, 59 exactly; TypeScript unknown narrowing per .agents/rules/typescript.md
    - `void init()`: suppresses floating-promise lint warning
    - Named export only: repo convention, enforced by CLAUDE.md and .agents/rules/typescript.md line 204-213
  </action>

  <verify>
    <automated>cd apps/tauri-todo && pnpm typecheck 2>&1 | tee /tmp/useTodos-typecheck.log; grep -q "error TS" /tmp/useTodos-typecheck.log && echo "TYPECHECK FAILED" || echo "TYPECHECK PASSED"</automated>
    <automated>cd apps/tauri-todo && pnpm lint 2>&1 | tee /tmp/useTodos-lint.log; (grep -E "(error|problem)" /tmp/useTodos-lint.log | grep -v "0 errors" && echo "LINT FAILED") || echo "LINT PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - File exists: test -f apps/tauri-todo/src/hooks/use-todos.ts
    - Contains export function useTodos: grep -q "^export function useTodos" apps/tauri-todo/src/hooks/use-todos.ts
    - Contains export type Todo: grep -q "^export type Todo = {" apps/tauri-todo/src/hooks/use-todos.ts
    - Contains export type TodosState: grep -q "^export type TodosState" apps/tauri-todo/src/hooks/use-todos.ts
    - Uses combined named-plus-type import for Store: grep -q 'import { load, type Store } from "@tauri-apps/plugin-store"' apps/tauri-todo/src/hooks/use-todos.ts
    - Uses useRef for store instance: grep -q "useRef<Store | null>(null)" apps/tauri-todo/src/hooks/use-todos.ts
    - Uses crypto.randomUUID: grep -q "crypto.randomUUID()" apps/tauri-todo/src/hooks/use-todos.ts
    - Uses cancelled flag: grep -q "let cancelled = false" apps/tauri-todo/src/hooks/use-todos.ts and grep -q "cancelled = true" apps/tauri-todo/src/hooks/use-todos.ts
    - Passes autoSave false and defaults empty: grep -q 'load("store.json", { autoSave: false, defaults: {} })' apps/tauri-todo/src/hooks/use-todos.ts
    - Uses null-coalesce for stored: grep -q "stored ?? \[\]" apps/tauri-todo/src/hooks/use-todos.ts
    - Error narrowing pattern present: grep -q "err instanceof Error ? err.message : String(err)" apps/tauri-todo/src/hooks/use-todos.ts
    - NO default export: ! grep -q "^export default" apps/tauri-todo/src/hooks/use-todos.ts
    - NO any used: ! grep -E ": any(\W|$)" apps/tauri-todo/src/hooks/use-todos.ts
    - NO bare inline import-type-alone: ! grep -E "^import \{ type " apps/tauri-todo/src/hooks/use-todos.ts (combined form `import { load, type Store }` is allowed and expected)
    - NO isTauriRuntime: ! grep -q "isTauriRuntime" apps/tauri-todo/src/hooks/use-todos.ts
    - File has at least 60 lines: test $(wc -l < apps/tauri-todo/src/hooks/use-todos.ts) -ge 60
    - cd apps/tauri-todo and pnpm typecheck exits 0 with no "error TS" output
    - cd apps/tauri-todo and pnpm lint exits 0 (no ESLint errors)
  </acceptance_criteria>

  <done>
    File apps/tauri-todo/src/hooks/use-todos.ts exists with the exact content above. The hooks/ directory was created. TypeScript typecheck passes with zero errors. ESLint passes with zero errors. Grep checks all pass (hook shape, imports, exports, ref usage, UUID, cancelled flag, error narrowing).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Rust greet command from lib.rs, keep store plugin registration</name>

  <files>
    apps/tauri-todo/src-tauri/src/lib.rs (MODIFY; strip greet function and invoke_handler line)
  </files>

  <read_first>
    - apps/tauri-todo/src-tauri/src/lib.rs (current file; must see the existing content before editing)
    - apps/tauri-todo/src-tauri/Cargo.toml (confirm tauri-plugin-store dep is present)
    - .planning/phases/02-todo-app/02-CONTEXT.md (D-09, remove greet command, no IPC commands in Phase 2)
    - .planning/phases/02-todo-app/02-RESEARCH.md (Pattern 3, Option B, omit invoke_handler entirely)
    - .planning/phases/02-todo-app/02-PATTERNS.md (lib.rs section, exact before/after diff)
  </read_first>

  <behavior>
    - Test 1: cargo check (run from apps/tauri-todo/src-tauri/) completes with zero errors and zero warnings
    - Test 2: The greet function no longer exists anywhere in the file
    - Test 3: The invoke_handler line no longer exists in the builder chain
    - Test 4: The plugin tauri_plugin_store Builder new build line IS still present
    - Test 5: The mobile entry point attribute cfg_attr(mobile, tauri::mobile_entry_point) is preserved
    - Test 6: The generate_context and expect lines are preserved
  </behavior>

  <action>
    Modify apps/tauri-todo/src-tauri/src/lib.rs to REPLACE the entire file contents with the EXACT 7-line content below (this is Option B from RESEARCH Pattern 3, cleanest, no warnings). The content to write:

    [RUST CODE START]
    #[cfg_attr(mobile, tauri::mobile_entry_point)]
    pub fn run() {
        tauri::Builder::default()
            .plugin(tauri_plugin_store::Builder::new().build())
            .run(tauri::generate_context!())
            .expect("error while running tauri application");
    }
    [RUST CODE END]

    Write exactly the 7 lines between markers (exclusive). Use 4-space indentation inside the function body.

    Changes being made (diff intent from current 13-line file to new 7-non-empty-line file):
    1. DELETE lines 1-4 (the tauri::command attribute plus fn greet body)
    2. DELETE line 10 (.invoke_handler(tauri::generate_handler![greet]))
    3. KEEP line 6 (cfg_attr mobile tauri::mobile_entry_point)
    4. KEEP line 7 (pub fn run open brace)
    5. KEEP line 8 (tauri::Builder::default)
    6. KEEP line 9 (.plugin tauri_plugin_store Builder new build)
    7. KEEP line 11 (.run tauri::generate_context)
    8. KEEP line 12 (.expect error while running tauri application)
    9. KEEP line 13 (closing brace)

    Use the Write tool to replace the entire file (simpler than a multi-line Edit), OR use the Edit tool with:
    - old_string: the full current file content (read it first to get exact whitespace)
    - new_string: the 7-line replacement above

    DO NOT:
    - Leave an empty invoke_handler generate_handler (Option A from research, valid but Option B is cleaner per RESEARCH line 446)
    - Remove the store plugin registration (PERS-01 requires it; verified in capabilities/mobile.json at store default)
    - Rename run or change its signature (Tauri mobile_entry_point macro expects this exact signature)
    - Add any new use statements (none needed; tauri_plugin_store is a direct crate reference)

    Why Option B over Option A:
    - RESEARCH Pattern 3 line 456: Tauri Builder does not require invoke_handler; if no commands exist, omit the call. This is cleaner than an empty macro and avoids any chance of a lint warning.
    - RESEARCH Pitfall 6 line 581: empty generate_handler is valid but may emit stylistic warnings in some Tauri versions; omitting the line entirely sidesteps this.
  </action>

  <verify>
    <automated>cd apps/tauri-todo/src-tauri && cargo check --manifest-path Cargo.toml 2>&1 | tee /tmp/cargo-check.log; (grep -E "^(error|warning)" /tmp/cargo-check.log | grep -v "compiling" && echo "CARGO FAILED") || echo "CARGO PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - File exists: test -f apps/tauri-todo/src-tauri/src/lib.rs
    - No greet function: ! grep -q "fn greet" apps/tauri-todo/src-tauri/src/lib.rs
    - No tauri::command attribute: ! grep -q "tauri::command" apps/tauri-todo/src-tauri/src/lib.rs
    - No invoke_handler call: ! grep -q "invoke_handler" apps/tauri-todo/src-tauri/src/lib.rs
    - No generate_handler: ! grep -q "generate_handler" apps/tauri-todo/src-tauri/src/lib.rs
    - Store plugin registered: grep -q "tauri_plugin_store::Builder::new().build()" apps/tauri-todo/src-tauri/src/lib.rs
    - Mobile entry point preserved: grep -q "cfg_attr(mobile, tauri::mobile_entry_point)" apps/tauri-todo/src-tauri/src/lib.rs
    - Run function preserved: grep -q "^pub fn run()" apps/tauri-todo/src-tauri/src/lib.rs
    - generate_context preserved: grep -q "tauri::generate_context!()" apps/tauri-todo/src-tauri/src/lib.rs
    - Expect call preserved: grep -q "error while running tauri application" apps/tauri-todo/src-tauri/src/lib.rs
    - File has exactly 7 non-empty lines: test $(grep -c . apps/tauri-todo/src-tauri/src/lib.rs) -eq 7
    - cd apps/tauri-todo/src-tauri and cargo check exits 0 with no error or warning lines (excluding Compiling status lines)
  </acceptance_criteria>

  <done>
    The greet Rust command is fully removed. lib.rs contains only the mobile entry point and the store plugin builder chain. cargo check passes with zero errors and zero warnings. All grep acceptance criteria pass. The file is 7 non-empty lines (down from 13).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to JS state | User-typed todo text enters React state via controlled input; never executed as code |
| JS hook to Tauri Store plugin | store.set crosses IPC to Rust-side plugin, which writes JSON to Android app_data_dir |
| Store plugin to filesystem | store.json written to Tauri-sandboxed app data directory (Android private storage) |

All boundaries are within the app sandbox. No network, no auth, no external services.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | store.json on-device file | accept | File lives in Tauri-sandboxed Android app_data_dir. Tampering requires root access on the device, which is out of scope for a single-user local todo app. No integrity signing needed. |
| T-02-02 | Information disclosure | Error messages from err narrowing | mitigate | Error messages are shown in the UI (Plan 02 TodoList error branch) with generic copy "Could not load todos. Restart the app and try again." The raw err.message from catch blocks is held in state but the error render branch uses generic copy (UI-SPEC line 207). Stack traces are never surfaced. |
| T-02-03 | Denial of service | Repeated addTodo calls with large text | accept | No input length cap in Phase 2 (D-01 minimal fields only, no validation beyond trim). Single-user local app, self-inflicted DoS only. |
| T-02-04 | Elevation of privilege | Tauri capabilities granting too-broad access | mitigate | capabilities/mobile.json grants only core default and store default, least-privilege for Phase 2. Do not add new permissions. Verified count of permissions in mobile.json is 2. |
| T-02-05 | Spoofing | N/A | accept | No authentication, no user identities. Single-user local app. |
| T-02-06 | Repudiation | N/A | accept | No audit log required. User is sole actor. |
</threat_model>

<verification>
## Plan-Level Verification

After both tasks complete:

1. cd apps/tauri-todo and pnpm typecheck exits 0 (validates hook types are correct)
2. cd apps/tauri-todo and pnpm lint exits 0 (validates import ordering, type-imports, no-any, no-default-export)
3. cd apps/tauri-todo/src-tauri and cargo check exits 0 with zero warnings (validates Rust compiles without greet command)
4. Files exist and directory structure is correct:
   - apps/tauri-todo/src/hooks/use-todos.ts
   - apps/tauri-todo/src-tauri/src/lib.rs (modified, not new)
5. Grep checks on both files pass (see acceptance_criteria for each task)

## Nyquist-Style Sanity Check (manual)

Since there is no test file for useTodos and Phase 2 does not add Vitest coverage (RESEARCH line 90: nyquist_validation false), the hook is validated by:
- TypeScript compiler catching type errors (strict mode plus noUncheckedIndexedAccess)
- ESLint catching style/convention violations
- Plan 03 Android device verification checkpoint exercising the hook end-to-end
</verification>

<success_criteria>
Plan 01 complete when:
1. apps/tauri-todo/src/hooks/use-todos.ts exists with the exact content specified, passes typecheck and lint
2. apps/tauri-todo/src-tauri/src/lib.rs has greet removed and invoke_handler removed, passes cargo check
3. Store plugin builder chain still registered in lib.rs
4. All grep acceptance criteria from both tasks pass
5. The hook exports useTodos, Todo, and TodosState, ready for Plan 02 components to import

Requirements addressed:
- TODO-01 (add todo): addTodo callback exposed
- TODO-02 (toggle): toggleTodo callback exposed
- TODO-03 (delete): deleteTodo callback exposed
- PERS-01 (plugin installed/registered): lib.rs still registers tauri_plugin_store Builder
- PERS-02 (persistence across restarts): Store.load on mount plus store.save after each mutation
- PERS-03 (capability granted): untouched, mobile.json already grants store default (verified in read_first)

Note: TODO-04 (empty state) is data-layer trivially true (empty array passes through) but UI-rendered in Plan 02.
</success_criteria>

<output>
After completion, create .planning/phases/02-todo-app/02-01-SUMMARY.md describing:
- Hook API signature (useTodos returns state plus addTodo plus toggleTodo plus deleteTodo)
- Exported types (Todo, TodosState)
- Rust cleanup completed
- Typecheck, lint, cargo check all passing
</output>
